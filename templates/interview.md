# 访谈门（Interview Gate）

> 创建日期: <YYYY-MM-DD>
>
> 原则：**猜错的代价 > 一次往返** 才阻塞提问；否则取默认 + 标注「假设」继续。
> 阻塞项**批量**用 `AskUserQuestion` 一次问清，别一个个挤牙膏。
> 未决阻塞不得进入阶段 4（写 Contracts）。

## 十二类逐项过（每类：要么问、要么取默认并标注）

| # | 类别 | 状态 | 结论 / 假设 |
|---|---|---|---|
| 1 | 交付形态（Web/小程序/CLI/服务/库，跑在哪，谁部署） | ☐问 ☐已答 ☐假设 | |
| 2 | 用户与角色（几种人、权限是否分层、谁看谁的数据） | | |
| 3 | 核心场景（最重要的三条旅程，逐步骤） | | |
| 4 | 规模与性能（数据量级、并发、响应上限） | | |
| 5 | 数据与持久化（存哪、什么形态、兼容既有 schema？迁移？） | | |
| 6 | 集成与外部依赖（对接什么系统、谁给凭据） | | |
| 7 | 权限与合规（个人信息/未成年人/金融/医疗？脱敏与审计） | | |
| 8 | **非目标（明确不做什么）** ← 跳过代价最大 | | |
| 9 | 成功定义（可量化的一句话） | | |
| 10 | 约束（技术栈是否指定、团队会什么、deadline、禁用依赖） | | |
| 11 | 视觉与交互（有设计稿吗？URL/.dc.html/截图；没有按什么风格） | | |
| 12 | 既有资产（要复用/兼容/替换的存量代码、数据、接口） | | |

## OPEN 问题清单

| OPEN-Q | 类别 | 描述 | 判定 | 处置 | 答复 |
|---|---|---|---|---|---|
| Q001 | 2 用户与角色 | 权限是否需要「部门及下级」层级 | 阻塞 | 问用户 | |
| Q002 | 8 非目标 | 是否包含数据导出 | 阻塞 | 问用户 | |
| Q003 | 4 规模 | 预期数据量级未知 | 可假设 | 默认 10 万行以内，标注 | |

## 提问草稿（喂给 AskUserQuestion，每问 2–4 个候选含推荐项）

1. 权限模型？ [四层级 个人/部门/部门及下级/全域(推荐) / 仅个人与管理员 / 无权限区分]
2. 本期是否包含导出功能？ [包含 / 不包含(推荐，排到 M2) / 只读不导]
3. …

## 结论

> 全部 OPEN 已确认或已假设 → 写「访谈门通过」入 `.pact/board.md`，进入写 P 层。
