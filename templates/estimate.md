# 工期与工作量估算

> 创建日期: <YYYY-MM-DD>
>
> **方法见 `<SKILL_DIR>/references/effort-estimation.md`，本文用它的固定输出格式。**
> 产出于 `S7 写 T 层`（估算门）。三条线写回 `T5`、停工线写回 `T3`、工期与团队写回 `P7`。
>
> **不得输出脱离本结构的单一「人日」数字。** 人日在 AI 开发下不是稳定单位。
> 要报量就报「模块数 + 阶段构成 + 阻塞缓冲」。

---

## 0. 估算前提

- **交付形态**：demo（5×）/ 产品级（2.5×） → `<填>`
- **执行者 AI 经验**：熟手（降本 55–65%）/ 生手（~30%） → `<填>`
- **项目类型**：新建 / 自有二开（~80%）/ 他人遗留改造（可能负收益） → `<填>`
- **② 类（未知业务约束）占比**：高 / 中 / 低 → `r₂ = 0.3 / 0.4 / 0.5`
  - 客观判据：`.pact/source-merge.md` 中「实测系统有、甲方文档无」的条目数 = `<N>` 条
- **推导**：`剩余时间比 = w×(1−r₁) + (1−w)×(1−r₂)` = `<x.xx>` → **降本 `<X>%`，加速 `<Y>×`**

**假设清单**（未确认的一律显式标注，不要静默填默认值）：

- `假设：…（未经确认）`

---

## 1. 模块清单

> 取 `A2` 模块职责表。中等 ×1；复杂折算 2–3 个中等，**必须写理由**。

| 模块 | 中等/复杂 | 折算当量 | 复杂原因 |
|---|---|---|---|
| | | | |

**合计中等当量：`<N>`**

> ⚠️ 若 `<N>` > 12，已超出 `effort-estimation.md` §四 快查表标定范围，
> 须写明「外推 `<N/12>` 倍，无本地实测支撑」。

---

## 2. 工期构成

| 项 | 天数 | 说明 |
|---|---|---|
| 阶段1 需求→PACT→原型 | 2–3 | 常数，不随模块数增长 |
| 阶段2 骨架生成 | 1–N | 常数，复杂系统取上限 |
| 阶段3 逐模块落地 | | **线性**：当量 × 1–2 天 |
| 变更预算 | +15~30% | 不留必超 |
| **开发工作量小计** | | 净投入 |
| 阻塞缓冲 ×1.5~2 | | **取并行最大值，不求和** |
| **交付周期** | | **对外只承诺这一行** |

---

## 3. 阻塞源清单

| 阻塞源 | 是否存在 | 责任方 | 最坏等待 | 合同条款 |
|---|---|---|---|---|
| 需求确认 | | | | |
| 客户反馈 | | | | |
| 第三方对接 | | | | |
| 数据 / 资料 | | | | |
| 审批 / 审核 | | | | |
| 现场排期 | | | | |

> 六类全部存在 → 缓冲取上限 ×2。

---

## 4. 三条线

- **骨架完成线**（可演示，**只覆盖约 50%，这不是完成**）：
- **开发完成线**（全模块调通、通过 `T1` 验收）：
- **交付线**（= 开发完成线 × 缓冲系数）：**← 对外只报这条**

---

## 5. 停工线（按不可追溯性排序）

> 工期不足时唯一合法判据是**可追溯性**，不是重要性。
> **禁止逐项降级**：范围可缩（整块不做），形态不可退化（同一个东西做得更差）。

**不可追溯，必须做到正确形态**（今天不做就永久缺失）：

1.

**可追溯补做，可整块推迟**（数据已存，可回溯执行）：

- 

> **停在第 `<K>` 条之前不可上线**，理由：`<…>`

---

## 6. 明确不含什么

- 

## 7. 无法估算的部分

> 缺 `T1` 验收标准的条目列在这里。**给数字比说不知道更有害。**

- 

---

## 8. 校准回填（项目进行中持续更新）

| 字段 | 预估 | 实际 | 偏差 |
|---|---|---|---|
| 阶段 1 实耗 | | | |
| 阶段 2 实耗 | | | |
| 每模块阶段 3 实耗（中等） | | | |
| 每模块阶段 3 实耗（复杂） | | | |
| **② 类事件数** | | | **最有预测价值的指标** |
| 阻塞净天数（按六类分记） | | | |
| 实际缓冲系数 | | | |

> **前 2 个模块的 ② 类事件数可外推整个项目的 r₂。**
> 若前两个模块就冒出大量隐藏规则，立即下调 r₂ 并重报周期，不要等到后期。
>
> **系数来源**：`先验表 / 本地实测（N 个项目）` → `<填>`。这决定结论能不能拿去签合同。

---

## 10. 并发方案（若考虑多人并行，方法见 `effort-estimation.md` §五点五）

> **并发压缩的是日历跨度，不是工作量。** 成本上升、工期下降，值不值取决于工期对你多贵。
> **先算关键路径地板再谈加人**——地板已接近目标工期时，加人纯属浪费预算。

### 10.1 并发天花板

`开发跨度(n) = S + (D−S)/n`；`交付周期(n) = 开发跨度 + D×(B−1)`（阻塞项与 n 无关）

| 人数 | 开发跨度 | 交付周期 | 压缩率 | 时间成本 |
|---|---|---|---|---|
| 1 | | | — | |
| 2 | | | | |
| 3 | | | | |
| 4 | | | | |

### 10.2 关键路径地板（无论多少人都突破不了）

> 沿 `P5` 依赖列做最长链分析，写出链路与跨越的模块当量。

```
<R00x → R0xx → …>
```
**地板：`<X>`–`<Y>` 天 ≈ `<x.x>`–`<y.y>` 个月**

### 10.3 公共模块清单（按传递扇入排序）

| R-ID | 传递扇入 | 占比 | 内容 | 里程碑 |
|---|---|---|---|---|
| | | | | |

> `PACT.md` 中声明为「单一来源 / 唯一入口」的（`C5`/`C6`/`C7`/`A4`）自动入列。

### 10.4 六条防冲突策略的项目化落地

| # | 策略 | 本项目怎么落 |
|---|---|---|
| 1 | 公共地基不并行，专人做完冻结再放人 | 哪几条 R-ID？串行头多少天？ |
| 2 | 垂直切分，禁止水平切分 | 按 `A2` 哪些模块目录划所有权？ |
| 3 | 契约冻结先于并发，改契约走 S10-CR | 冻结哪些 `C1`/`C5`/`C6` 条目？ |
| 4 | `@pact` 标注 + `pact-trace.sh` 每日跑 | |
| 5 | 共享护栏跑 CI | 哪几条 `C3` 不变量可做成 CI 检查？ |
| 6 | 按依赖层排并发波次 | 各层宽度？哪几层别塞人？ |

### 10.5 置信度声明（必写）

> 并发部分（Amdahl 串行比、沟通开销、集成返工率）**是推导值，不是实测**。
> **先按 2 人跑完第一个里程碑，回填实际并行效率，再决定要不要加第 3 人。**
> 额外回填：实际串行头天数、跨人返工次数、契约变更次数。
