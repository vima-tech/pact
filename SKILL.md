---
name: pact
description: >
  PACT = Product · Architecture · Contracts · Tests。把 PRD / SDD / SPEC / 验收标准 / 施工范围
  熔成**一份单文件完备规格 `PACT.md`**，并以**强制工序协议**驱动它落地。
  设计目标不是好读，而是**让一个此前对本项目一无所知的人或 AI，只读这一份文件就能开工——
  不追问背景、不猜测意图、不遗漏约束**。
  用于新建项目开局与大需求设计。执行方式不是"参考流程"，而是 **S0–S11 十二道工序**：
  每道有进入条件、必须产出、可机检的退出条件；不适用的工序必须显式标「已跳过（理由）」，
  禁止静默略过。四道机检兜底：`pact-status.sh` 查工序顺序、`pact-lint.sh` 查规格完备性、
  `pact-trace.sh` 查**代码是否真按规格实现**（规格 ↔ 代码 `@pact` 标注 ↔ 覆盖表三方交叉比对，
  抓虚报与野生功能）、`pact-book.sh --check` 查**生成的知识库是否与真源漂移**；
  再由「零知识冷读门」验证"只读一份就能开工"。
  交付物除单文件真源外，还自动生成知识库 `.pact/pact-book/`：**md 原文给 AI 施工**
  （每条需求一页，聚合需求+验收+依赖+决策+契约位置）、**自研单文件 HTML 给人查阅**
  （双击即开、零依赖、零外部请求，可直接发给甲方；三栏布局、字段感知搜索 ms:/src:/类型:、
  ★与里程碑过滤、R-ID 悬停预览、可点击依赖图、明暗主题）；另有里程碑工作包与
  R-ID / D-ID / 不变量 / 来源 / 依赖图谱等反查索引。不依赖 mdbook。
  内建 **AI 辅助开发的工期估算门**（`S7`）：50% 法则（骨架只覆盖一半且提不上去）、
  降本推导（熟手 55–65% / 生手 ~30%）、**驱动因子分层法**（把需求按机器可判的规则分
  T1/T2/T3 三层，配可持续费率卡，取代主观的模块当量；T3 常占条数 15–20% 却吃掉 55–65% 工期）、
  阻塞缓冲 ×1.5–2、多人并发天花板（3 人只快约 28%）、
  输出骨架/开发完成/交付三条线（对外只承诺交付线）、按可追溯性给停工线——
  **禁止凭直觉给工期或报价数字**，那会被当成承诺。
  内建工程纪律：R-ID 可追溯矩阵、批量审核门、多来源差异逐条裁定、存量代码八维评估、
  产物收敛落盘、结构与配置集中、交付即可被人类或 AI 接手。
  触发词：pact、PACT 文档、完备规格、新项目开局、大需求设计、写 PRD/SDD/SPEC、
  技术方案设计、规格评审、冷读检查、需求冻结、按工序施工、规格知识库、
  工期评估、工作量评估、报价、排期、这个系统多久能做完、AI 能提速多少。
argument-hint: "[--help] [<简报文本 | 既有文档路径...>] [--new|--feature|--merge|--build|--audit] [--level=full|feature] [--review]"
---

# PACT — 单文件完备规格 + 强制工序协议

**PACT 既是一种文档体裁，也是一条不许跳步的流水线。** 你要产出并落地一份 `PACT.md`。

它的验收标准只有一条，且必须被真实检验：
> 一个对本项目**一无所知**的人或 AI，**只读这一份文件**即可开始编码，
> 且不需要追问背景、不需要猜测意图、不会遗漏约束。

**纪律高于速度**：不问清不开写，不冻结不施工，不过冷读门不算完备，不过状态机检不算完工。

## 与相邻体裁的边界（决定了什么该写进去）

| 体裁 | 它回答 | PACT 的不同 |
|---|---|---|
| `CLAUDE.md` | **怎么写代码**（偏好与惯例） | PACT 回答**写什么、为什么、做到什么算完成** |
| PRD | 要什么 | PACT 必须再给出可执行的接口、表结构、状态机 |
| SDD | 怎么设计（假设读者已知业务背景） | PACT **不做此假设**，且显式记录**决策理由**与**已否决方案** |
| SPEC | 通常只覆盖接口 | PACT 同时承载动机、约束与验收 |

> 推论：凡是「懂行的人自然知道」的东西，PACT 里**必须写出来**。
> 凡是「怎么排版、用什么命名 idiom」的东西，属于 `CLAUDE.md`，**不进 PACT**。

## 四层（缺一层即不成立）

| 层 | 内容 | 锚点 |
|---|---|---|
| **P**roduct | 背景、用户、场景、需求、非目标、约束、成功定义 | `P1–P8` |
| **A**rchitecture | 结构、模块职责、设计原则、**带理由的决策与已否决方案** | `A1–A6` |
| **C**ontracts | 数据模型、接口、状态机、配置、权限、观测、prompt 契约 | `C1–C11` |
| **T**ests | 验收清单、指标、停工线、交付前置、施工范围 | `T1–T5` |

**章节编号与标题可按项目自由调整，但两件事不可变：**
1. 每个必备条目上方带机器可读锚点 `<!-- PACT:C4 -->`（lint 靠它判定完备性）。
2. 文档头部必须有「**四层 → 本文位置**」映射表（人靠它导航）。

---

# ★ 执行协议（硬性 · 本节优先级高于下文一切描述）

## 协议零：`--help` 短路（先于其余一切判断）

**若 `$ARGUMENTS` 含 `--help` / `-h` / `帮助` / `怎么用`，只做一件事：把使用速览原样输出给用户，
然后停止。** 不判模式、不建 `.pact/`、不碰 `PACT.md`、不进入任何工序。

```bash
SKILL_DIR="$(ls -d ./.claude/skills/pact ~/.claude/skills/pact 2>/dev/null | head -1)"
bash $SKILL_DIR/scripts/pact-help.sh
```

（脚本不可用时，直接读 `<SKILL_DIR>/references/help.md` 并原样呈现。）
输出后可以补一句「想开始就说 `/pact <你的需求>`」，**但不要自作主张替用户开工**。

## 协议一：十二道工序，逐道收敛

```
S0 定位摄入 → S1 访谈门 → [S2 熔合门] → [S3 存量评估]
   → S4 写P → S5 写A → S6 写C → S7 写T
   → S8 完备性门 → S9 冻结 → S10 施工LOOP → S11 收尾门
```

| 工序 | 名称 | 何时触发 | 必须产出（落盘） | **退出条件（全满足才准进下一道）** |
|---|---|---|---|---|
| **S0** | 定位与摄入 | 总是 | `.pact/` 骨架、`board.md` 工序表、`PACT.md` 起步 | 模式已判定并说明依据；骨架文件齐备非空；`PACT.md` 头部已填创建日期 |
| **S1** | 访谈门 | 除 `--build/--audit` | `.pact/interview.md` | 十二类逐项过完；每个 OPEN-Q 状态 ∈ {已答, 已假设}；无 `阻塞` 残留 |
| **S2** | 熔合门 | 有 ≥2 份既有物料 | `.pact/source-merge.md` | 全部 `DIFF` 已裁定；每条非平凡裁定已落成 `A5` 的 D-ID（含已否决） |
| **S3** | 存量评估 | 有实质代码 | `.pact/assessment.md` | 八维全部评完并给证据 `file:line`；P0 已排进 `T5` 的 M0 |
| **S4** | 写 P 层 | 总是 | `PACT.md` `P1–P8` | `P5` 有 R-ID 且横切项全过；`P6` 非目标非空；`P8` 可量化 |
| **S5** | 写 A 层 | 总是 | `PACT.md` `A1–A6` | 每个非平凡取舍都有 D-ID 四件套（选项/结论/理由/已否决） |
| **S6** | 写 C 层 | 总是 | `PACT.md` `C1–C11` | 每条契约是表格或代码块；不适用者写 `N/A（理由）`；无「到时候再定」 |
| **S7** | 写 T 层 | 总是 | `PACT.md` `T1–T5`、`.pact/estimate.md`（估算门） | 每个 R-ID 在 `T1` 有可执行验收；`T3` 停工线非空；`T5` 里程碑含「明确不含」；**估算门已过或在 `board.md` 标『已跳过（理由）』** |
| **S8** | 完备性门 | 总是 | `.pact/cold-read.md`、`.pact/pact-book/` | ① `pact-lint.sh` exit 0 ② 物料反扫无遗漏 ③ **冷读门 PASS** ④ `pact-book.sh` 生成成功且 `--check` 无漂移 |
| **S9** | 冻结 | 除 `--audit` | `PACT.md` 头部状态、`.pact/changelog.md` | 头部标 `状态: 已冻结 · <日期>`；changelog 已建 |
| **S10** | 施工 LOOP | 除 `--audit` | 代码（每段带 `@pact R###` 标注）+ `.pact/coverage.md` | `coverage.md` 全部 R-ID `已验证`；`C3` 不变量通过；**`pact-trace.sh --require-complete` 通过** |
| **S11** | 收尾自检门 | 除 `--audit` | 自检结果写入 `board.md`、`.pact/pact-book/` 已重生成 | `T4` 逐条打勾；末次冷读通过；**四道机检全绿**（含 `pact-book.sh --check`） |

**条件工序（S2/S3）不适用时，必须在 `board.md` 标 `已跳过（理由）`。静默略过 = 违反协议。**

模式对工序的裁剪：
- `--build`：S0 → S10 → S11（S1–S9 标 `已跳过（PACT 已冻结）`）
- `--audit`：S0 → S8，出报告即止，**不改任何实现**
- `--merge`：S2 必做
- `--feature`：S3 必做，且 `--level=feature`

## 协议二：每轮三件事（防上下文压缩失忆）

对话上下文会被压缩，**你的记忆不可信，落盘的文件才可信**。每一轮回复都照做：

1. **开工先读** `.pact/board.md` —— 确认当前在第几道工序、上轮做到哪、有无阻塞。
   **绝不凭记忆判断进度。**
2. **只做当前这一道工序** —— 不提前做下一道，不回头重做已收敛的。
3. **收工必写** `.pact/board.md` —— 更新工序状态、本轮做了什么、下一道是什么、阻塞是什么。
   **board.md 没更新 = 这一轮不算做完。**

## 协议三：断点续跑

会话中断、上下文被压缩、或换了一个 agent 接手时，**唯一真相源按此顺序读**：

```bash
cat .pact/board.md          # ① 工序状态表 → 我在第几道
cat PACT.md | head -30      # ② 头部状态 → 是否已冻结
bash <SKILL_DIR>/scripts/pact-status.sh   # ③ 机检：骨架齐备性 + 工序顺序合法性 + 下一道
git status                  # ④ 有无未收尾的改动
```

读完这四样再决定做什么。**不要重新访谈、不要重写已冻结的规格、不要重复已完成的工序。**

## 协议四：四道机检（自称完成不算数）

```bash
SKILL_DIR="$(ls -d ./.claude/skills/pact ~/.claude/skills/pact 2>/dev/null | head -1)"

bash $SKILL_DIR/scripts/pact-status.sh          # 工序：骨架 + 状态合法性 + 顺序 + 下一道
bash $SKILL_DIR/scripts/pact-lint.sh PACT.md    # 规格：四层完备性九项检查
bash $SKILL_DIR/scripts/pact-trace.sh           # 落地：规格 ↔ 代码 ↔ 覆盖表 三方交叉比对
bash $SKILL_DIR/scripts/pact-book.sh --check    # 视图：知识库 ↔ PACT.md 是否漂移
```

四道各管一件事，缺一不可：

| 脚本 | 回答的问题 | 抓什么 |
|---|---|---|
| `pact-status.sh` | **工序走到哪了、有没有跳步** | 跳步、静默略过、冻结状态不一致 |
| `pact-lint.sh` | **PACT 写够了没有** | 锚点缺失、占位符、R-ID 无验收、决策无「已否决」 |
| `pact-trace.sh` | **代码真按 PACT 做了没有** | **虚报**（覆盖表说已验证但代码里没有）、**野生功能**（代码有 R-ID 但规格里没有）、漏登记、未实现 |
| `pact-book.sh --check` | **知识库还是不是 PACT.md 的忠实投影** | 手改生成物、改了 `PACT.md` 忘了重生成、生成物残留孤儿文件 |

**在 S8、S10 每轮、S11，以及任何你打算说「完成了」的时候，这四个脚本都必须真跑并贴结果。**
没跑过就宣称完成 = 违反协议。

> `pact-book.sh` 还会顺带报出**结构性缺陷**，这些是 lint 看不出来的（lint 查结构，它查图的连通性）：
> 无 `T1` 验收的需求、**未排入任何里程碑的需求**、指向不存在需求的依赖、缺「已否决」的决策。
> 详见生成的 `.pact/pact-book/src/idx/健康度.md`。改完 `PACT.md` 记得重新 `--build`。

## 协议五：十条禁令

1. **禁止跳过工序。** 不适用的工序要在 `board.md` 标 `已跳过（理由）`，不得静默略过。
2. **禁止 `board.md` 未更新就进入下一道工序。**
3. **禁止在 S8 未 PASS 时写业务代码。** 规格没冻结就施工 = 带病开工。
4. **禁止写没有 R-ID 的功能，也禁止写了不标注。** 每段业务代码必须带 `@pact R###` 注释
   （见 S10 标注约定），否则 `pact-trace.sh` 无法反查，覆盖表就成了一句空话。
   用户中途加需求也不例外——必走 **S10-CR 五步**
   （回 `P5` → 补 `T1` → 改 `C` 层 → 记 changelog → 判断是否重跑冷读门）。
5. **禁止自行裁定多来源分歧。** 矛盾类必须 `AskUserQuestion` 让用户定，不得"择优采用"。
6. **禁止把状态只留在对话里。** 一律落盘到 `PACT.md` / `.pact/`。
7. **禁止为通过测试而改宽断言。** 失败先查根因。这是 `T3` 停工线。
8. **禁止未跑机检就宣称完成**（协议四）。
9. **禁止冻结后改规格却不记 `changelog.md`。** 规格漂移是最贵的债。
10. **禁止用「大致」「应该」「后续再定」搪塞 C 层。** 契约层写不出来就回 S1 问清楚。
11. **禁止不走估算门就给工期或报价数字。** 见协议七。凭直觉给的数字会被当成承诺，
    且必然漏掉阻塞缓冲（×1.5–2）与后 50% 的落地量。
12. **禁止用骨架完成线对外承诺。** 骨架只覆盖约 50%，且缺的是最容易出事的那半。
13. **禁止手改 `.pact/pact-book/` 下的任何文件。** 它是 `PACT.md` 的生成物，手改必被 `--check` 抓出、
    并在下次 `--build` 覆盖。要改内容就改 `PACT.md` 再重新生成——
    **知识库好查是副产品，真源唯一才是目的。**

## 协议六：什么时候该停下来问人

只在「**猜错的代价 > 一次往返**」时阻塞提问；能合理假设的就假设、标注 `假设` 继续。
必须停下来问的四种情况：

- S1 访谈门里判定为 `阻塞` 的 OPEN-Q；
- S2 熔合门里的**矛盾**类分歧（细化类可默认采精确方并标注）；
- S10 施工中冒出的、无法合理假设的关键歧义；
- 命中 `T3` 任一条停工线。

提问方式：**批量** `AskUserQuestion`（每问 2–4 个候选含推荐项），narration 写 `needs input:` 一行。
别一个个挤牙膏。

## 协议七：估算门（`S7` 内执行）

**任何人问「这个系统多久能做完 / 工作量多大 / 能不能在 X 之前上线 / 报个价」，
不许凭直觉给数字——凭直觉给的数字会被当成承诺。** 按 `references/effort-estimation.md` 走。

```bash
cat $SKILL_DIR/references/effort-estimation.md   # 方法
cp  $SKILL_DIR/templates/estimate.md .pact/      # 产出模板
```

**触发**：需对外报价、承诺排期、立项评估、需求变更影响评估时**必做**；
纯内部无排期压力的项目可在 `board.md` 标 `估算门：已跳过（理由）`——静默略过 = 违反协议。

**五条硬规矩**（细节见方法文档）：

1. **先定四个前提**：交付形态（demo 5× / 产品级 2.5×）、执行者 AI 经验（熟手 55–65% / 生手 ~30%）、
   项目类型（新建 / 自有二开 ~80% / 他人遗留改造**可能负收益**）、② 类未知业务约束占比（定 r₂）。
   **缺一个就不许出数字。**
2. **数模块，不数功能点。** 模块清单取 `A2`；中等 ×1–2 天，复杂折 2–3 个中等并写理由。
   **只有阶段 3 随规模线性增长**，骨架时间可忽略。
3. **输出三条线**：骨架完成线（只覆盖 **50%**，不是完成）／开发完成线（过 `T1`）／
   交付线（× 阻塞缓冲 1.5–2，**取并行最大值不求和**）。**对外只承诺交付线。**
4. **停工线按可追溯性排序**，写回 `T3`。不可追溯的（数据契约、采集保真度、审计留痕）必须做到正确形态；
   可追溯补做的（报表、界面观感、分析深度）可整块推迟。
   **禁止逐项降级**——范围可缩，形态不可退化，半成品留下的是错误数据和必然返工。
5. **`P5` 里有条目缺 `T1` 验收就停止估算**，回答「无法估算，缺哪几条」。
   给数字比说不知道更有害。

> **最容易搞反的一条**：**PACT 写得好不是下调 `T5` 工期的理由。**
> 规格从 60 分写到 90 分，AI 骨架覆盖率只从 50% 提到约 55%，边际收益极低。
> PACT 消除的是**方向性返工与验收扯皮**，不是编码量。时间预算必须显性留给后 50%。

---

# 输入与模式

参数 `$ARGUMENTS`：一段口头简报、一个想法、或若干既有文档/代码路径。模式自动判定，也可显式指定：

| 模式 | 触发 | 干什么 |
|---|---|---|
| `--new` | 空项目 / 只有一句想法 | **新建项目开局**：访谈补白 → 从零写完整 PACT → 冻结 → 施工 |
| `--feature` | 已有代码库 + 一个大需求 | **大需求设计**：存量对齐 → 写特性级 PACT（`--level=feature`）→ 冻结 → 施工 |
| `--merge` | 给了既有 PRD/SDD/原型/散落文档 | **熔合**：多来源差异逐条裁定 → 合成单份 PACT |
| `--build` | 已有冻结的 `PACT.md` | 跳过写作，直接进 S10 施工闭环 |
| `--audit` | 已有 `PACT.md` | 只体检：S8 三道门，出报告不改实现 |
| `--help` | 任何时候 | **只打印使用速览就停**（协议零），不执行任何工序 |

`--review`：每道闸门后暂停等确认（默认不停，但产物始终落盘，可随时打断）。
`--level=full|feature`：完备度档位。`full`=新建项目（30 个锚点全填）；
`feature`=大需求（核心集必填，其余可 `N/A（理由）`）。默认按模式推断。

## 产物骨架（**必须存在**）

```
<project>/
  PACT.md            # 【真源】单文件完备规格。冻结后改动须走 .pact/changelog.md
  CLAUDE.md          # 【必须】怎么写代码：惯例、门禁、目录导览。指向 PACT.md
  README.md          # 入口：一句话 + 怎么跑 + 指向 PACT.md 与 .pact/
  .pact/             # 【必须】过程与执行态全部收敛于此，不在根目录摊开
    board.md         #   ★工序状态表 + 进度看板（断点续跑的第一真相源）
    interview.md     #   S1 访谈门：OPEN-Q 清单与裁定记录
    source-merge.md  #   S2 熔合门：多来源差异比对与逐条裁定
    assessment.md    #   S3 存量代码八维评估
    estimate.md      #   S7 估算门：工期/工作量/三条线/停工线（对外报价只用「交付线」）
    cold-read.md     #   S8 零知识冷读门报告（每次追加，保留历史）
    changelog.md     #   S9 后：PACT 变更记录（改了什么、为什么、影响哪些 R-ID）
    coverage.md      #   S10 R-ID 覆盖审计表（R-ID → 实现位置 → 验收 → 状态）
    baseline/*.png   #   设计基准截图（有 UI 时）
    pact-book/       # 【生成物】知识库（S8 起生成，勿手改）
      pact-book.html # ★ 单文件知识库：双击即开，零依赖、可直接发给甲方/分包方
      src/           #   md 原文 —— **AI 施工读这里**，HTML 只面向人
      book.toml      #     可选：装了 mdbook 也能 `mdbook serve`（非必需）
      src/SUMMARY.md #     mdbook 导航
      src/{p,a,c,t}/ #     按 30 个锚点切分的章节页
      src/r/R###.md  #   ★ 每条需求一页：需求+验收+依赖+决策+契约位置（AI 施工素材）
      src/m/M#.md    #     里程碑工作包：该阶段全部需求 + 出口条件 + 明确不含
      src/idx/*.md   #     反查索引：R-ID / D-ID / 不变量 / 来源 / 依赖图谱 / 健康度
```

**分工铁律**：
- `PACT.md` = **冻结的规格**（做什么、为什么、算完成）——**不外链到别处才看得懂**。
- `.pact/` = **过程与执行态 + 生成视图**（问了什么、评估了什么、做到哪了、生成了什么）——
  可变，不影响 PACT 的自足性。**根目录只留 `PACT.md` / `CLAUDE.md` / `README.md` 三个入口文件**，
  其余一律收进 `.pact/`——知识库虽是交付物，但它是生成的、可随时重建，放根目录会喧宾夺主。
- `CLAUDE.md` = **惯例**（怎么写）。
- `.pact/pact-book/` = **`PACT.md` 的生成视图**（好查、好切片）——**不是真源，一个字都不许手写**。
  其中 `src/` 是 **AI 施工读的 md**，`pact-book.html` 是**给人看的单文件网页**（自研渲染，不依赖 mdbook）。

> **为什么知识库必须是生成的、而不是手写的第二份文档**：
> PACT 自己第 9 条禁令就是「规格漂移是最贵的债」。两份手写内容必然漂移，
> 届时没人知道该信哪份。生成 + `--check` 门禁，让漂移在 CI 里就被抓住，而不是在验收现场。
>
> **冷读门永远只读 `PACT.md`**：知识库再好查，也不能替代「只读一份就能开工」这条验收标准。
> 若某条信息只有在知识库里跳三页才拼得出来，那是 `PACT.md` 写漏了，不是知识库的功劳。

### 本 skill 自带的资源

> **路径约定**：下表路径**相对本 `SKILL.md` 所在目录**，下文写作 `<SKILL_DIR>/...`。
> 实际位置取决于安装方式（项目级 `.claude/skills/pact/`、全局 `~/.claude/skills/pact/`，
> 或其他 agent 的 skills 目录）。**执行脚本前先确定 `<SKILL_DIR>`**：
> ```bash
> SKILL_DIR="$(ls -d ./.claude/skills/pact ~/.claude/skills/pact 2>/dev/null | head -1)"
> ```

| 路径 | 用途 |
|---|---|
| `references/agent-protocol.md` | **★ 十二张工序卡**：每道工序的动作清单、判定标准、常见偷懒模式。**每道工序开工前读对应那张卡** |
| `references/help.md` | **人类向使用速览**。`--help` 时原样输出这份 |
| `references/authoring-guide.md` | 逐节「写到什么程度算够」+ 反例。写不下去或不确定精度时读它 |
| `references/example-PACT.md` | 通过全部机检的 feature 级完整范例 |
| `templates/PACT.md` | 30 锚点骨架，起手就拷这个 |
| `templates/{board,interview,source-merge,assessment,cold-read,coverage,changelog,CLAUDE}.md` | 各工序模板 |
| `scripts/pact-status.sh` | **工序机检**：骨架齐备 + 状态合法 + 顺序合法 + 告诉你下一道 |
| `scripts/pact-lint.sh` | **规格机检**：四层完备性九项检查。零依赖 |
| `scripts/pact-trace.sh` | **落地机检**：规格 ↔ 代码 `@pact` 标注 ↔ 覆盖表三方交叉比对，抓虚报与野生功能。零依赖 |
| `references/effort-estimation.md` | **AI 辅助开发的工期与工作量评估法**：50% 法则、降本推导、模块当量、阻塞缓冲、三条线、停工线。`S7` 估算门执行它 |
| `templates/estimate.md` | 估算门产出模板（固定输出格式） |
| `scripts/pact-book.sh` | **生成知识库**（默认 `--build`）：md 原文 + **单文件 HTML**；`--check` 查漂移。底层 `pact-book.mjs` + `pact-book-html.mjs`，只需 node |
| `vendor/marked.min.js` | 单文件 HTML 内嵌的 markdown 渲染器（MIT，40KB）。章节页搬运 `PACT.md` 正文，作者可写任意 markdown，手搓解析器必踩坑 |
| `scripts/pact-help.sh` | 打印使用速览（`--help` 用；人类也可在终端直接跑） |
| `scripts/token-lint.sh` | 有 UI 时：组件里禁止裸 hex/px/rgb。零依赖 |
| `scripts/visual-diff.mjs` | 有 UI 时：截图对基准图 pixelmatch。需 `playwright pixelmatch pngjs` |
| `scripts/computed-style.spec.ts` | 有 UI 时：断言 computed 值 == token 值的测试模板 |

---

# 工序详述

## 工序 S0 · 定位与摄入（只读，先搞清楚在给什么项目写规格）

1. **判模式**：探测 cwd 是否有实质代码（非空 `src/`/`backend/`/`apps/` 等）、是否已有 `PACT.md`、
   `$ARGUMENTS` 是否指向既有文档 → 定模式，**在开场一句话里说明判定结果与依据**。
2. **读全部给定物料**：既有 PRD/SDD/设计稿/README/历史文档，以及（存量项目）实际代码。
   **以运行代码和配置为事实来源**——历史 Markdown 可能描述了尚未实现或已改名的能力。
3. **建骨架**：创建 `.pact/`，从 `templates/` 拷起步文件（**`board.md` 必须含工序状态表**）；
   `PACT.md` 从 `templates/PACT.md` 起步。
4. **物料归位**（既有文档不在 `.pact/` 下时）：按「**先查引用 → 再移动 → 改引用 → 验证**」四步走。
   `grep -rn` 找出代码/测试/脚本/配置/文档里对这些路径的全部引用（含 `../../docs/...`、
   `CARGO_MANIFEST_DIR.join(...)`、import、md 链接），移动后同步改引用，再跑一次构建/测试确认没断。
   **跳过"先查引用"这一步会断掉被代码引用的样本包**——这是有过教训的坑。
   不可移动的外部只读物料 → 不强搬，在 `PACT.md` 的 `A1` 记其外部位置。
5. **写日期**：`PACT.md` 头部填 `创建日期: YYYY-MM-DD`（当前实际日期）。
   后续实质性修订追加 `更新日期:`。**无日期的规格文档不可信**。

**退出条件**：模式已判定并说明依据 · 骨架文件齐备非空 · 创建日期已填 · `board.md` 工序表已建。

---

## 工序 S1 · 访谈门（新建项目最不该跳的一道）

从一句想法直接开写规格，产出的必然是**把模型自己的默认假设伪装成用户需求**的文档。

按 `templates/interview.md` 的**十二类**逐项过一遍，每类要么问、要么取默认并标注：

1. **交付形态**：Web / 小程序 / CLI / 服务 / 库？谁部署、跑在哪？
2. **用户与角色**：几种人用？权限是否分层（个人/部门/部门及下级/全域）？谁能看谁的数据？
3. **核心场景**：最重要的三条用户旅程，从入口到完成的每一步。
4. **规模与性能**：数据量级、并发、响应时间上限。
5. **数据与持久化**：存哪、什么形态、有无既有 schema 要兼容、迁移策略。
6. **集成与外部依赖**：要对接什么系统/云服务？谁提供凭据？
7. **权限与合规**：涉及个人信息/金融/医疗/未成年人？脱敏与审计要求。
8. **非目标**：**明确不做什么**（跳过这一项的代价最大——范围会无声膨胀）。
9. **成功定义**：怎么算做成了？可量化。
10. **约束**：技术栈是否被指定？团队会什么？deadline？不能引入的依赖？
11. **视觉与交互**：有设计稿吗（URL / `.dc.html` / 截图）？没有的话按什么风格？
12. **既有资产**：有没有要复用/兼容/替换的存量代码、数据、接口？

**动作**：每个 OPEN-Q 标 `阻塞` 或 `可假设`，记入 `.pact/interview.md`。
有 `阻塞` → 批量 `AskUserQuestion` + `needs input:`。
`可假设` → 取默认值写进 PACT 并标 `假设：<默认值>（未经确认）`，继续，不打扰用户。

> 闸门可**提前触发**：某个阻塞在 S0 就已确凿（如完全没说给谁用），立刻问，别等汇总。

**退出条件**：十二类逐项过完 · 每个 OPEN-Q ∈ {已答, 已假设} · 无 `阻塞` 残留。

---

## 工序 S2 · 熔合门（条件：有 ≥2 份既有物料）

既有物料往往不止一份，且互相打架。**「更精准」≠「自动覆盖」**：
每一处分歧都要显式上桌、由用户裁定，**未裁定不得写进 PACT**。

来源优先级默认（**只用来给建议，不用来替用户做决定**）：
**运行中的代码 > SDD/技术方案 > 原型设计 > PRD > 历史文档**。

汇总到 `.pact/source-merge.md`，每条记：
`DIFF-ID | 主题 | 来源A 怎么说(引用) | 来源B 怎么说(引用) | 分歧类型 | 影响的 R-ID | 建议 | 裁定`

分歧类型至少分四类：
- **矛盾**：直接冲突（字段/阈值/流程/状态机不同）→ **必须裁定**。
- **细化**：一方在另一方之上补了精度 → 通常采精确方，仍列出确认。
- **缺漏**：一方有另一方无 → 确认是否仍要实现。
- **越界**：某来源引入了别处没有的需求/范围 → 确认是否纳入。

**动作**：`AskUserQuestion` 批量呈现高影响分歧，候选形如
`以 SDD 为准(推荐)` / `以 PRD 为准` / `两者都要` / `都不要`。裁定后：
1. 结论写进 PACT 正文（成为规格，**不留"两种说法"**）；
2. **每条非平凡裁定在 `A5` 落一条 D-ID，落选方案写进「已否决」**——
   这正是 PACT 区别于 SDD 的地方：后人能看到路口，而不只是看到结果。

**退出条件**：全部 `DIFF` 已裁定 · 每条非平凡裁定已落 D-ID · 无"两种说法"残留在 PACT 里。

---

## 工序 S3 · 存量代码八维评估（条件：有实质代码 · 只读不改码）

**不要凭想象设计**，先把现有实现摸清，PACT 才能与现实接得上。

1. **实现盘点**：模块/分层/语言/规模；现有代码 ↔ 待写需求的关联点。
2. **八维评估**（每维给：现状 / 问题 / 证据 `file:line` / 严重度 P0–P2），落 `.pact/assessment.md`：
   1. 结构与分层是否清晰、目录职责是否守住
   2. **核心不变量**：本系统的"静默腐蚀点"——逐条查实现是否成立
   3. 既有裁定/约定在代码里是否被遵守（冲突 = P0）
   4. 数据正确性铁律（精度、单位、时区、校验门、字符归一）
   5. 安全与权限：RBAC、数据隔离、敏感字段脱敏、操作留痕
   6. 可维护性与技术债：重复、死代码、配置散落、过度耦合
   7. 性能与可扩展：分页、N+1、大数据路径、热点
   8. 测试与文档：关键逻辑有无测试、能否回归、能否接手
3. **结论回流到 PACT**：现状约束 → `P7`；结构 → `A2`；
   必须先修的 P0 → `T5` 的 M0；与新需求冲突的存量设计 → `A5` 的 D-ID（改还是绕，理由是什么）。

> **结构整理是 P1，不是 P2**：根目录散落文件、文档与实际不符，属「影响可接手性」，
> **必须在本轮处理**，不得降级为可推迟的打磨项。

**退出条件**：八维全部评完并给证据 `file:line` · 结论已回流到 PACT 对应锚点 · P0 已排进 `T5` 的 M0。

---

## 工序 S4 · 写 P 层（`P1–P8`）

写给「一无所知的人」看。**禁止出现只有内部人懂的黑话而不加解释。**

- `P1` 一句话定义与交付形态：这是什么、给谁、以什么形态交付。
- `P2` 背景与问题：现在没有它会怎样、痛在哪、为什么现在做。**不写这段，后人无法判断取舍。**
- `P3` 用户与角色：每个角色一行——是谁、要干什么、**能看/能改什么**。
- `P4` 核心场景：三条以内关键旅程，逐步骤写（入口 → 每步 → 完成态 → **异常分支**）。
- `P5` 需求清单：**R-ID 矩阵，PACT 的脊椎**。
  - 含「且/或/除非/当…时」的句子**必须拆成多条**。
  - 示例、表格、状态机、接口字段 = **一等需求**，逐条反推规则。
  - **横切项逐一过并落条目**：权限/角色、输入校验、边界、空状态、错误处理、幂等/并发、
    **破坏性操作二次确认**、分页/性能、审计日志、国际化。缺则显式写「本项目不需要，因为…」。
  - 末尾写覆盖声明：「来源 X 全文 N 段已全部归类，无遗漏」。
- `P6` 非目标：**明确不做什么，逐条写 + 为什么不做**。这一节空着 = 范围失控。
- `P7` 约束：每条注明是**硬约束**还是**倾向**。
- `P8` 成功定义：可量化。「用户满意」不算，「四个权限层级各自只看到应看数据」算。

**退出条件**：`P5` 有 R-ID 且横切项全过 · `P6` 非空 · `P8` 可量化 · 覆盖声明已写。

---

## 工序 S5 · 写 A 层（`A1–A6`）

- `A1` 系统边界与上下文：负责什么、**不负责什么**、外部依赖、**依赖失效时的行为**。
- `A2` 结构与模块职责：目录树 + 每个目录职责 + 关联 R-ID。这是后续代码的落位真源。
- `A3` 关键链路：端到端流转（谁调谁、数据怎么变、**权限在哪一跳裁定**、错误怎么冒泡）。
- `A4` 设计原则：本项目的硬规矩，要**具体到能据以驳回一个 PR**。
- `A5` 决策记录（D-ID）：**PACT 的灵魂**。每条四件套，缺一不可：

  ```
  #### D001 · <决策标题>
  - **选项**：A… / B… / C…
  - **结论**：采 B
  - **理由**：<为什么。要能被反驳，不能只写"更好""更现代">
  - **已否决**：A——<否决理由>；C——<否决理由>
  ```

  什么该成为 D-ID：技术选型、数据模型关键取舍、S2 的每条裁定、
  与存量冲突时的处理方式、任何「三个月后有人会问为什么这么做」的地方。
  **推翻旧决策时**：新增一条 D-ID 说明为什么推翻，旧条目标 `已被 D0xx 取代`，**不要删**。
- `A6` 风险与缓解：已知会咬人的地方 + 触发信号 + 缓解手段。

**退出条件**：每个非平凡取舍都有 D-ID 四件套 · 「理由」经得起反驳 · `A2` 能让人知道新代码放哪。

---

## 工序 S6 · 写 C 层（`C1–C11`）—— 精度门槛在这里

**判定标准：读完这一层，实现者不需要再做任何设计决策，只需要打字。**
凡是「到时候再定」的地方，要么现在定，要么回 S1 问。

- `C1` 数据模型：表/集合、每字段的名称·类型·可空·默认值·含义、索引、关联、**历史数据兼容**。
- `C2` 枚举与状态机：所有枚举完整取值；`状态 × 事件 → 新状态` **全表**，含**非法迁移的处理**。
- `C3` 不变量与数据铁律：必须永真的断言。每条应能对应到 `T1` 的一个检查。
- `C4` 对外接口契约：路径/入参(含校验)/出参结构/错误情形/鉴权/幂等 + **真实请求响应示例**。
- `C5` 内部模块接口：跨模块调用约定（无则 `N/A（理由）`）。
- `C6` 错误码与错误语义：码 → 含义 → 用户可见文案 → 调用方该怎么办。
- `C7` 配置与环境变量：名称、用途、默认值、是否敏感、单一来源。**敏感值不进仓库，必须有 `.env.example`。**
- `C8` 权限与安全边界：谁能看/改什么、**服务端在哪一行做裁定**、数据隔离范围、越权行为。
  **空权限范围必须返回空结果**，而不是因过滤条件为 null 而放开全部数据。
- `C9` 观测与日志契约：打什么、什么级别、**哪些字段必须脱敏**。
- `C10` 交互与呈现契约：页面清单、设计 token、组件状态矩阵（`variant × size × state`，漏格=漏需求）、
  类型阶梯。实现只引用 token，**禁止裸 hex/px**。无 UI → `N/A（理由）`。
- `C11` AI/Prompt 契约：prompt 模板、输入输出 schema、模型与参数、失败与降级、幻觉/越权兜底。
  无 AI → `N/A（理由）`。

> **契约必须可执行**：每个条目要么是表格，要么是代码块/schema。
> 散文式的「接口应返回用户信息」不算契约，lint 会拦。

**退出条件**：每条契约是表格或代码块 · 不适用者写 `N/A（理由）` · 全文无「到时候再定」。

---

## 工序 S7 · 写 T 层（`T1–T5`）

- `T1` 验收清单：**每个 R-ID 必须有一行**——`R### | 验收方式 | 判定标准 | 检查者`。
  验收方式要具体到能执行（测试名/命令/操作步骤）。**没有验收方式的需求 = 不算需求**，回 `P5` 补或删。
- `T2` 指标与阈值：性能、覆盖率、视觉 diff 阈值、错误率，每项给数字 + 测量方式。
- `T3` 停工线：**出现即停、不得继续施工**的客观信号。通用起点：
  权限过滤被绕过 · 真实个人信息进了日志或仓库 · 数据不可逆丢失 ·
  核心不变量被破坏 · **测试为了通过而被改宽**。再加项目特有的。
- `T4` 交付前置（DoD）：交付前必须全绿的可勾选清单。
- `T5` 施工范围与里程碑：`M0/M1/M2…`，每个给：包含的 R-ID、出口条件、**明确不含什么**。
  存量项目的 P0 修复排 M0。

**退出条件**：每个 R-ID 在 `T1` 有可执行验收 · `T3` 非空且客观可判定 · `T5` 每个里程碑有「明确不含」。

---

## 工序 S8 · 完备性门（硬闸门，三道全过才准冻结）

**① 机检 lint**
```bash
# --level 必须与 PACT.md 头部「完备度档位」一致
bash <SKILL_DIR>/scripts/pact-lint.sh PACT.md --level=full      # 新建项目
bash <SKILL_DIR>/scripts/pact-lint.sh PACT.md --level=feature   # 大需求
```
九项：四层锚点齐备且非空 · 无占位符 · 每个 R-ID 被 `T1` 覆盖 · 每个 D-ID 有理由与已否决 ·
`C1`/`C4` 含表格或代码块 · 头部有创建日期与四层映射表。**FAIL 必须修到 PASS，不许豁免。**

**② 自查反扫**
拿全部输入物料（简报、既有 PRD/SDD、访谈记录、存量代码）**反向重扫**对照 PACT，
列出 未覆盖 / 部分覆盖 / 偏离 的条目。有则补。

**③ 零知识冷读门 —— PACT 的定义性检验**

> **另起一个全新的 agent，只给它 `PACT.md`（不给对话历史、不给其他文档、不给代码库背景），
> 让它输出：(a) 它理解的实现计划；(b) 它必须追问才能动工的问题清单；(c) 它发现的矛盾与歧义。**

用 `Agent` 工具（`subagent_type: general-purpose`）跑，prompt 见 `templates/cold-read.md`。

| 冷读结果 | 判定 | 动作 |
|---|---|---|
| 追问清单为空，且实现计划与你的意图一致 | **PASS** | 可冻结 |
| 它提出的问题，答案其实**已在 PACT 里** | 不算漏洞，但**表述不可发现** | 调结构/加交叉引用，重跑 |
| 它必须追问背景、意图、或任何约束 | **FAIL** | 每个问题回填到对应锚点，重跑 |
| 它的实现计划**跑偏** | **FAIL** | `P4/P5/P6` 表述有歧义，改写后重跑 |

报告追加到 `.pact/cold-read.md`。**跑到 PASS 为止**，不许"差不多了"。
`--level=full` 建议跑两个视角：「实现者」（能不能开工）+「反对者」（哪里自相矛盾/不可实现）。

**退出条件**：lint exit 0 · 反扫无遗漏 · 冷读门 PASS 且报告已落盘。

---

## 工序 S9 · 冻结

1. `PACT.md` 头部标 `状态: 已冻结 · <日期>`。
2. 建 `.pact/changelog.md`。
3. `board.md` 记「S8 三道门全过，已冻结」。

此后 PACT 的任何改动**必须**在 changelog 记一行（改了什么、为什么、影响哪些 R-ID），
并重跑受影响部分的 lint。**涉及 `P1/P2/P4/P5/P6`（意图与范围层）的改动必须重跑冷读门。**

`--review` 模式：冻结前停下等用户确认。

**退出条件**：头部状态已改 · changelog 已建 · board 已记录。

---

## 工序 S10 · 施工 LOOP（按 R-ID 逐单元实现 + 自动验收）

工作单元 = **一个里程碑内的一簇 R-ID**（有 UI 时通常 = 一个页面 + 其 R-ID 簇；
存量项目 = 一个 P0/P1 优化项）。按 `T5` 的里程碑顺序取下一个未完成单元。
**P0 全清前不碰打磨项。**

每轮：
1. **实现**：代码落到 `A2` 约定的位置；配置进 `C7` 的集中配置层，业务代码里零散落魔法值。
   **每段业务代码必须带 R-ID 标注**，格式固定（任何语言的注释里都能写）：

   ```
   // @pact R001              单个
   // @pact R001,R002         一段代码覆盖多个
   # @pact R014               Python / Shell
   <!-- @pact R021 -->        模板 / HTML
   ```

   这不是装饰——`pact-trace.sh` 靠它反查「R001 实现在哪」，
   并识别**虚报**（覆盖表说做完了但代码里没有）与**野生功能**（代码有但规格里没有）。
2. **验收**（读 PASS/FAIL，不靠肉眼）：
   - 跑 `T1` 为该簇 R-ID 指定的验收方式。
   - 有 UI 时追加三道：
     ```bash
     bash <SKILL_DIR>/scripts/token-lint.sh <组件目录>        # 裸 hex/px/rgb 必须为 0
     node <SKILL_DIR>/scripts/visual-diff.mjs <页面URL> <基准图> --threshold=0.02
     # + computed-style 断言，模板见 <SKILL_DIR>/scripts/computed-style.spec.ts
     ```
     `visual-diff.mjs` 需 `playwright pixelmatch pngjs`；项目若已有自己的视觉回归方案，
     直接用你自己的，别为跑这个脚本引入新依赖。
   - 核对 `C3` 中与本单元相关的不变量仍然成立。
   - **跑可追溯性机检**：`bash <SKILL_DIR>/scripts/pact-trace.sh`
     —— 本轮实现的 R-ID 必须出现在代码标注里，且无虚报、无野生 R-ID。
3. **判定**：任一 FAIL → 同一单元继续修，**不前进**；全过 → `.pact/coverage.md` 标该簇 `已验证`。
4. **停工线检查**：对照 `T3`，命中任一条 → **立即停**，`needs input:` 说明命中了哪条。
5. 每轮末更新 `.pact/board.md`；PACT 有变动同步 changelog。

> 施工中冒出**新的关键歧义**且无法合理假设 → 回 S1 提问（`needs input:`），别带着猜测硬写。
> **节奏**：连续工作，不必睡眠。仅在等构建/dev-server 时用 `ScheduleWakeup`
> （短轮询 ≤270s；长等待 ≥1200s）并回传同样的 `/pact` 入参续跑。

### S10-CR · 施工中的需求变更（冻结后用户改需求，必走此流程）

真实项目里**一定**会发生：规格冻结了，用户说「再加个功能」「这里改一下」。
**此时不要直接改代码**——绕过规格改代码，PACT 立刻退化成一份过时的摆设文档。

五步，一步不能省：

1. **回 `P5`** 加/改 R-ID（新需求给新号，**不复用已删除的号**）。
   若变更同时影响 `P6` 非目标（原本说不做的现在要做了），一并更新。
2. **回 `T1`** 给这个 R-ID 补可执行验收。**没有验收方式就不算需求。**
3. **若影响契约**（表结构/接口/错误码/配置/权限）→ 同步改 `C` 层对应锚点。
4. **记 `.pact/changelog.md`**：日期 / 改了哪个锚点 / 改成什么 / **为什么** / 影响哪些 R-ID。
5. **判断是否重跑门禁**：
   - 改到 `P1/P2/P4/P5/P6`（意图与范围层）→ **必须重跑 S8 冷读门**；
   - 只改 `C` 层字段细节且不动语义 → 跑 `pact-lint.sh` 即可；
   - 变更**推翻了某条决策** → `A5` 新增 D-ID 说明为什么推翻，
     旧条目标 `已被 D0xx 取代`，**不要删**。

**范围判断**：变更若超出当前里程碑，回 `T5` 排进后续里程碑，
**不要塞进正在做的单元**——里程碑无声膨胀是交付延期的头号原因。
变更若与 `P6` 非目标或 `P7` 硬约束冲突 → 停下来 `AskUserQuestion`，不要自行放宽约束。

**退出条件**：`coverage.md` 全部 R-ID `已验证` · `C3` 不变量检查通过 · 无未处理的停工线命中 ·
本轮所有需求变更都已走完 S10-CR 五步 ·
`pact-trace.sh --require-complete` 通过（P5 每个 R-ID 都能在代码里反查到，无虚报、无野生）。

---

## 工序 S11 · 收尾自检门（全绿才算完成）

1. `.pact/coverage.md` 无 `状态≠已验证` 的 R-ID；`T1` 每条验收都**真跑过并贴结果**。
2. 拿 PACT 原文**反向重扫**实现，列出 未覆盖/部分覆盖/偏离。
3. `T2` 各项指标达阈值（有 UI 则逐页贴 diff% vs 阈值）。
4. `T4` 交付前置清单逐条打勾。
5. **规格与实现一致性**：PACT 里的契约（表结构/接口/错误码/配置）与代码实测一致；
   不一致 → 要么改代码，要么改 PACT 并记 changelog，**不许放着**。
6. **四道机检全绿**（协议四，含 `pact-book.sh --check`）：
   ```bash
   bash <SKILL_DIR>/scripts/pact-status.sh
   bash <SKILL_DIR>/scripts/pact-lint.sh PACT.md --level=<full|feature>
   bash <SKILL_DIR>/scripts/pact-trace.sh --require-complete
   ```
   `--require-complete` 会把「还有 R-ID 未实现」从提示升级为 FAIL —— 收尾时必须加。
7. **可接手性自检**：`A2` 与实际目录一致 · 配置集中且有 `.env.example` ·
   新人照 README 能一把跑通 · 无孤儿/临时/死文件 · 无 `TODO` 占位 ·
   **根目录仅入口文件 + 业务目录**。
8. **末次冷读**：对最终 PACT 再跑一次冷读门，确认它描述的仍是**现在的系统**（防规格与实现漂移）。
9. 全绿 → 输出 `result:` 一行总结。否则回 S10 继续 loop。

**退出条件**：以上 1–8 全部满足。

---

# 贯穿纪律

**规格纪律**
- PACT **自足**：不靠外链、不靠对话历史、不靠"你懂的"。引用外部文档时必须摘录关键结论进正文。
- **决策留痕**：每个非平凡取舍都有 D-ID，写清**已否决方案与否决理由**。
- **可追溯双向**：R-ID ↔ 代码位置 ↔ 验收方式，三者互指。
- **冻结后走 changelog**：规格漂移是最贵的债。
- **文档必须带日期**：`创建日期` 必填，实质性修订加 `更新日期`。

**工程纪律**
- 遵循现有项目结构；空项目才提新约定。一文件一职责，按特性/领域分目录，
  不堆 `utils`/`helpers` 万能文件。
- 配置分层且各有单一来源；敏感值不进仓库；业务代码零硬编码魔法值。
- 权限过滤必须在服务端查询层；前端菜单/按钮隐藏**不是**安全边界。
- 涉及个人信息一律脱敏，测试与文档里不得出现真实数据。
- **移动任何文件/目录必走四步**：先 grep 查全部引用 → 移动 → 同步改引用 → 跑构建/测试验证。

**输出纪律（这是后台 loop）**
- 每轮开头一句「当前第几道工序 · 要做什么」，结尾一句「结果 + 下一道」。
- 关键数字（覆盖率、lint 结果、各页 diff%）用自己的话复述，别只丢工具输出。
- 有未决**阻塞**时 `needs input:` 独占一行说明缺什么，并用 `AskUserQuestion` 给选项。
- 完成时 `result:` 独占一行、自包含。被用户打断则先复述其指令再行动。
- **默认不提交代码**：除非用户明确要求，改完停在未提交状态。
