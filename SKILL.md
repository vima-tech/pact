---
name: pact
description: >
  PACT = Product · Architecture · Contracts · Tests。把 PRD / SDD / SPEC / 验收标准 / 施工范围
  熔成**一份单文件完备规格 `PACT.md`**，并驱动其落地。设计目标不是好读，而是
  **让一个此前对本项目一无所知的人或 AI，只读这一份文件就能开工——不追问背景、不猜测意图、不遗漏约束**。
  用于新建项目开局与大需求设计：先访谈补白 → 写四层规格（含决策理由与已否决方案）→
  过「零知识冷读门」（另起一个只读 PACT 的 agent，它必须追问的每一个问题都是规格漏洞）→
  冻结 → 按 R-ID 施工 + 自动验收，直到覆盖表全绿。
  内建工程纪律：R-ID 可追溯矩阵、批量审核门、多来源差异逐条裁定、
  存量代码八维评估、产物收敛落盘、结构与配置集中、交付即可被人类或 AI 接手。
  触发词：pact、PACT 文档、完备规格、新项目开局、大需求设计、写 PRD/SDD/SPEC、
  技术方案设计、规格评审、冷读检查、需求冻结。
argument-hint: "[<简报文本 | 既有文档路径...>] [--new|--feature|--merge|--build|--audit] [--level=full|feature] [--review]"
---

# PACT — 单文件完备规格：先把「做什么」写到能开工，再施工

**PACT 是一种文档体裁，也是一条流水线。** 你要产出并落地一份 `PACT.md`。

它的验收标准只有一条，且必须被真实检验：
> 一个对本项目**一无所知**的人或 AI，**只读这一份文件**即可开始编码，
> 且不需要追问背景、不需要猜测意图、不会遗漏约束。

**纪律高于速度**：不问清不开写，不冻结不施工，不过冷读门不算完备。

## 与相邻体裁的边界（决定了什么该写进去）

| 体裁 | 它回答 | PACT 的不同 |
|---|---|---|
| `CLAUDE.md` | **怎么写代码**（偏好与惯例） | PACT 回答**写什么、为什么、做到什么算完成** |
| PRD | 要什么 | PACT 必须再给出可执行的接口、表结构、状态机 |
| SDD | 怎么设计（假设读者已知业务背景） | PACT **不做此假设**，且显式记录**决策理由**与**已否决方案** |
| SPEC | 通常只覆盖接口 | PACT 同时承载动机、约束与验收 |

> 推论：凡是「懂行的人自然知道」的东西，PACT 里**必须写出来**。
> 凡是「怎么排版、用什么命名 idiom」的东西，属于 `CLAUDE.md`，**不进 PACT**。

## 四层（缺一层即不成立）

| 层 | 内容 | 锚点 |
|---|---|---|
| **P**roduct | 背景、用户、场景、需求、非目标、约束、成功定义 | `P1–P8` |
| **A**rchitecture | 结构、模块职责、设计原则、**带理由的决策与已否决方案** | `A1–A6` |
| **C**ontracts | 数据模型、接口、状态机、配置、权限、观测、prompt 契约 | `C1–C11` |
| **T**ests | 验收清单、指标、停工线、交付前置、施工范围 | `T1–T5` |

**章节编号与标题可按项目自由调整，但两件事不可变：**
1. 每个必备条目上方带机器可读锚点 `<!-- PACT:C4 -->`（lint 靠它判定完备性）。
2. 文档头部必须有「**四层 → 本文位置**」映射表（人靠它导航）。

---

## 输入与模式

参数 `$ARGUMENTS`：一段口头简报、一个想法、或若干既有文档/代码路径。模式自动判定，也可显式指定：

| 模式 | 触发 | 干什么 |
|---|---|---|
| `--new` | 空项目 / 只有一句想法 | **新建项目开局**：访谈补白 → 从零写完整 PACT → 冻结 → 施工 |
| `--feature` | 已有代码库 + 一个大需求 | **大需求设计**：存量对齐 → 写特性级 PACT（`--level=feature`）→ 冻结 → 施工 |
| `--merge` | 给了既有 PRD/SDD/原型/散落文档 | **熔合**：多来源差异逐条裁定 → 合成单份 PACT（裁定结论写成 D-ID 决策） |
| `--build` | 已有冻结的 `PACT.md` | 跳过写作，直接进阶段 6 施工闭环 |
| `--audit` | 已有 `PACT.md` | 只体检：四层齐备 + lint + 冷读门，出报告不改实现 |

`--review`：每道闸门后暂停等确认（默认不停，但产物始终落盘，可随时打断）。
`--level=full|feature`：完备度档位。`full`=新建项目（30 个锚点全填）；
`feature`=大需求（核心集必填，其余可 `N/A（理由）`）。默认按模式推断。

## 产物骨架（**必须存在**）

```
<project>/
  PACT.md            # 【真源】单文件完备规格。冻结后改动须走 .pact/changelog.md
  CLAUDE.md          # 【必须】怎么写代码：惯例、门禁、目录导览。指向 PACT.md
  README.md          # 入口：一句话 + 怎么跑 + 指向 PACT.md 与 .pact/
  .pact/             # 【必须】过程与执行态全部收敛于此，不在根目录摊开
    interview.md     #   访谈门：OPEN-Q 清单与裁定记录
    source-merge.md  #   多来源差异比对与逐条裁定（--merge / 存量对齐时）
    assessment.md    #   存量代码八维评估（--feature 时）
    cold-read.md     #   零知识冷读门报告（每次跑都追加，保留历史）
    changelog.md     #   PACT 冻结后的变更记录（谁改了什么、为什么、影响哪些 R-ID）
    board.md         #   进度看板（当前阶段 / 工作单元队列 / 阻塞）
    coverage.md      #   R-ID 覆盖审计表（R-ID → 实现位置 → 验收 → 状态）
    baseline/*.png   #   设计基准截图（有 UI 时）
```

**分工铁律**：
- `PACT.md` = **冻结的规格**（做什么、为什么、算完成）——需求、契约、决策、验收都在里面，**不外链到别处才看得懂**。
- `.pact/` = **过程与执行态**（问了什么、评估了什么、做到哪了）——可变、可脏、不影响 PACT 的自足性。
- `CLAUDE.md` = **惯例**（怎么写）。

> 严禁把状态只留在对话上下文里（会被压缩遗忘）——一律落到上述文件。

### 本 skill 自带的资源

> **路径约定**：下表路径**相对本 `SKILL.md` 所在目录**，下文写作 `<SKILL_DIR>/...`。
> 实际位置取决于安装方式（项目级 `.claude/skills/pact/`、全局 `~/.claude/skills/pact/`，
> 或其他 agent 的 skills 目录）。**执行脚本前先确定 `<SKILL_DIR>`**，例如：
> ```bash
> SKILL_DIR="$(ls -d ./.claude/skills/pact ~/.claude/skills/pact 2>/dev/null | head -1)"
> ```

| 路径 | 用途 |
|---|---|
| `templates/PACT.md` | 30 锚点骨架，起手就拷这个 |
| `templates/{interview,source-merge,assessment,cold-read,board,coverage,changelog,CLAUDE}.md` | 各闸门与执行态模板 |
| `references/authoring-guide.md` | **逐节「写到什么程度算够」+ 反例**。写不下去或不确定精度时读它 |
| `references/example-PACT.md` | **通过全部机检的 feature 级完整范例**。看 `A5` 的理由/已否决怎么写、`N/A` 怎么带理由 |
| `scripts/pact-lint.sh` | 完备性机检（见阶段 6）。零依赖，只需 bash |
| `scripts/token-lint.sh` | 有 UI 时：组件里禁止裸 hex/px/rgb。零依赖 |
| `scripts/visual-diff.mjs` | 有 UI 时：截图对基准图 pixelmatch。需 `playwright pixelmatch pngjs` |
| `scripts/computed-style.spec.ts` | 有 UI 时：断言 computed 值 == token 值的测试模板 |

---

## 阶段 0 · 定位（只读，先搞清楚在给什么项目写规格）

1. **判模式**：探测 cwd 是否有实质代码（非空 `src/`/`backend/`/`apps/` 等）、是否已有 `PACT.md`、
   `$ARGUMENTS` 是否指向既有文档 → 定 `--new / --feature / --merge / --build / --audit`，
   并在开场一句话里说明判定结果与依据。
2. **读全部给定物料**：既有 PRD/SDD/设计稿/README/历史文档，以及（存量项目）实际代码。
   **以运行代码和配置为事实来源**，历史 Markdown 可能描述了尚未实现或已改名的能力。
3. **建骨架**：创建 `.pact/`，从 `templates/` 拷起步文件；`PACT.md` 从 `templates/PACT.md` 起步。
4. **物料归位**（既有文档不在 `.pact/` 下时）：按「**先查引用 → 再移动 → 改引用 → 验证**」四步走，
   `grep -rn` 找出代码/测试/脚本/配置/文档里对这些路径的全部引用（含 `../../docs/...`、
   `CARGO_MANIFEST_DIR.join(...)`、import、md 链接），移动后同步改引用，再跑一次构建/测试确认没断。
   不可移动的外部只读物料 → 不强搬，在 `PACT.md` 的 A1 记其外部位置。
5. **写日期**：`PACT.md` 头部填 `创建日期: YYYY-MM-DD`（用当前实际日期）。
   后续实质性修订追加 `更新日期:`。**无日期的规格文档不可信**——后人无法判断它是否已过期。

---

## 阶段 1 · 访谈门（Interview Gate · 新建项目的关键闸门）

**这是 `--new` 最容易被跳过、也最不该跳过的一步。** 从一句想法直接开写规格，
产出的必然是把模型自己的默认假设伪装成用户需求的文档。

**原则**：只在「猜错的代价 > 一次往返」时才阻塞提问；能合理假设的就假设、
写进 PACT 的 `假设` 标注继续。**所有阻塞项批量用 `AskUserQuestion` 一次问清，别一个个挤牙膏。**

按 `templates/interview.md` 的**十二类**逐项过一遍，每类要么问、要么取默认并标注：

1. **交付形态**：Web / 小程序 / CLI / 服务 / 库？谁部署、跑在哪？
2. **用户与角色**：有几种人用？权限是否分层（个人/部门/全域）？谁能看谁的数据？
3. **核心场景**：最重要的三条用户旅程，从入口到完成的每一步。
4. **规模与性能**：数据量级、并发、响应时间上限。
5. **数据与持久化**：存哪、什么形态、有无既有 schema 要兼容、迁移策略。
6. **集成与外部依赖**：要对接什么系统/云服务/支付/短信？谁提供凭据？
7. **权限与合规**：涉及个人信息/未成年人/金融/医疗？脱敏与审计要求。
8. **非目标**：**明确不做什么**（这一项被跳过的代价最大——范围会无声膨胀）。
9. **成功定义**：怎么算做成了？可量化的一句话。
10. **约束**：技术栈是否被指定？团队会什么？deadline？不能引入的依赖？
11. **视觉与交互**：有设计稿吗（URL / `.dc.html` / 截图）？没有的话按什么风格？
12. **既有资产**：有没有要复用/兼容/替换的存量代码、数据、接口？

**动作**：
- 每个 OPEN-Q 标 `阻塞` 或 `可假设`，记入 `.pact/interview.md`。
- 有任一 `阻塞` → `AskUserQuestion` **批量**提问（每问 2–4 个候选，含推荐项），
  narration 里写 `needs input:` 一行。**未获答复不得进入阶段 4（写 Contracts）**。
- `可假设` → 取默认值，写进 PACT 对应条目并标 `假设：<默认值>（未经确认）`，继续，不打扰用户。
- 全部 OPEN 清零后写「访谈门通过」入 `.pact/board.md`。

> 闸门可**提前触发**：某个阻塞在阶段 0 就已确凿（如完全没说给谁用），立刻问，别等汇总。

---

## 阶段 1.5 · 多来源熔合门（`--merge` / 存在多份既有文档时强制）

既有物料往往不止一份，且互相打架。**「更精准」≠「自动覆盖」**：
每一处分歧都要显式上桌、由用户裁定，**未裁定不得写进 PACT**。

来源优先级默认（可被裁定推翻）：**运行中的代码 > SDD/技术方案 > 原型设计 > PRD > 历史文档**。
但优先级只用来给**建议**，不用来替用户做决定。

汇总到 `.pact/source-merge.md`，每条记：
`DIFF-ID | 主题 | 来源A 怎么说(引用) | 来源B 怎么说(引用) | 分歧类型 | 影响的 R-ID | 建议 | 裁定`

分歧类型至少分四类：
- **矛盾**：直接冲突（字段定义/阈值/流程/状态机不同）→ 必须裁定。
- **细化**：一方在另一方之上补了精度 → 通常采精确方，仍列出确认。
- **缺漏**：一方有另一方无 → 确认是否仍要实现。
- **越界**：某来源引入了别处没有的需求/范围 → 确认是否纳入。

**动作**：`AskUserQuestion` 批量呈现高影响分歧，候选形如
`以 SDD 为准(推荐)` / `以 PRD 为准` / `两者都要` / `都不要`。
裁定后：
1. 结论写进 PACT 对应条目（成为规格正文，不留"两种说法"）；
2. **每条非平凡裁定都要在 `A5 决策记录` 里落一条 `D-ID`，把落选方案写进「已否决」**
   —— 这正是 PACT 区别于 SDD 的地方：后人能看到路口，而不只是看到结果。

---

## 阶段 1.8 · 存量代码八维评估（`--feature` 强制 · 只读不改码）

**触发**：已有实质代码。此时**不要凭想象设计**，先把现有实现摸清，PACT 才能与现实接得上。

1. **实现盘点**：模块/分层/语言/规模；现有代码 ↔ 待写需求的关联点。
2. **八维评估**（每维给：现状 / 问题 / 证据 `file:line` / 严重度 P0–P2），落 `.pact/assessment.md`：
   1. 结构与分层是否清晰、目录职责是否守住
   2. **核心不变量**：本系统的"静默腐蚀点"——逐条查实现是否成立
   3. 既有裁定/约定在代码里是否被遵守（冲突 = P0）
   4. 数据正确性铁律（精度、单位、时区、校验门、字符归一）
   5. 安全与权限：RBAC、数据隔离、敏感字段脱敏、操作留痕
   6. 可维护性与技术债：重复、死代码、配置散落、过度耦合
   7. 性能与可扩展：分页、N+1、大数据路径、热点
   8. 测试与文档：关键逻辑有无测试、能否回归、能否接手
3. 结论回流到 PACT：
   - 摸清的**现状约束**写进 `P7 约束` 与 `A2 结构`；
   - 必须先修的 P0 写进 `T5 施工范围` 的第一个里程碑；
   - 与新需求冲突的存量设计写成 `D-ID` 决策（改还是绕，理由是什么）。

> **结构整理是 P1，不是 P2**：根目录散落文件、文档与实际不符，属「影响可接手性」，
> 必须在本轮处理，不得降级为可推迟的打磨项。根目录应仅留入口文件 + 业务目录。

---

## 阶段 2 · 写 P 层（Product · P1–P8）

写给「一无所知的人」看。**禁止出现只有内部人懂的黑话而不加解释。**

- `P1 一句话定义与交付形态`：这是什么、给谁、以什么形态交付。
- `P2 背景与问题`：现在没有它会怎样、痛在哪、为什么现在做。**不写这段，后人无法判断取舍。**
- `P3 用户与角色`：每个角色一行——是谁、要干什么、能看/能改什么（权限层级在此定基调）。
- `P4 核心场景`：三条以内关键旅程，逐步骤写（入口 → 每一步 → 完成态 → 异常分支）。
- `P5 需求清单`：**R-ID 矩阵，PACT 的脊椎**。逐条原子化：
  `R### | 类型 | 描述(可判真假) | 验收标准(可量化) | 优先级 | 依赖 | 来源 | 假设`
  - 含「且/或/除非/当…时」的句子**必须拆成多条**。
  - 示例、表格、状态机、接口字段 = **一等需求**，逐条反推规则。
  - **横切项逐一过并落条目**：权限/角色、输入校验、边界、空状态、错误处理、
    幂等/并发、**破坏性操作二次确认**、分页/性能、审计日志、国际化。缺则显式写「本项目不需要，因为…」。
  - 末尾写覆盖声明：「来源 X 全文 N 段已全部归类，无遗漏」。
- `P6 非目标`：**明确不做什么，逐条写**。这一节空着 = 范围失控。
- `P7 约束`：技术栈/合规/资源/环境/存量兼容，每条注明是**硬约束**还是**倾向**。
- `P8 成功定义`：可量化。「用户满意」不算，「首屏 < 1.5s 且四个权限层级各自只看到应看数据」算。

---

## 阶段 3 · 写 A 层（Architecture · A1–A6）

- `A1 系统边界与上下文`：本系统负责什么、**不负责什么**、外部依赖有哪些、集成点在哪。
- `A2 结构与模块职责`：目录树 + 每个目录职责 + 关联 R-ID。这是后续代码的落位真源。
- `A3 关键链路`：至少画出主链路的端到端流转（谁调谁、数据怎么变、在哪落库、错误怎么冒泡）。
- `A4 设计原则`：本项目的硬规矩（如「权限过滤必须发生在服务端查询层，前端菜单不是安全边界」）。
- `A5 决策记录（D-ID）`：**PACT 的灵魂**。每条决策四件套，缺一不可：

  ```
  #### D001 · <决策标题>
  - **选项**：A… / B… / C…
  - **结论**：采 B
  - **理由**：<为什么。要能被反驳，不能只写"更好">
  - **已否决**：A——<否决理由>；C——<否决理由>
  ```

  什么该成为 D-ID：技术选型、数据模型的关键取舍、阶段 1.5 的每条裁定、
  与存量冲突时的处理方式、任何「三个月后有人会问为什么这么做」的地方。
- `A6 风险与缓解`：已知会咬人的地方 + 触发信号 + 缓解手段。

---

## 阶段 4 · 写 C 层（Contracts · C1–C11）—— 精度门槛在这里

**判定标准：读完这一层，实现者不需要再做任何设计决策，只需要打字。**
凡是「到时候再定」的地方，要么现在定，要么回阶段 1 问。

- `C1 数据模型`：表/集合、每个字段的名称·类型·可空·默认值·含义、索引、关联关系。
- `C2 枚举与状态机`：所有枚举的完整取值；状态机给 `状态 × 事件 → 新状态` 全表，含非法迁移的处理。
- `C3 不变量与数据铁律`：必须永真的断言（「同一实体不会同时处于 A 和 B 态」「金额不跨币种相加」）。
  每条不变量应能对应到 T 层的一个检查。
- `C4 对外接口契约`：每个端点/函数——路径或名称、入参（含校验规则）、出参结构、
  错误情形、鉴权要求、幂等性。**给真实的请求/响应示例。**
- `C5 内部模块接口`：跨模块调用约定（无则 `N/A（理由）`）。
- `C6 错误码与错误语义`：码 → 含义 → 用户可见文案 → 调用方该怎么办。
- `C7 配置与环境变量`：每项——名称、用途、默认值、是否敏感、单一来源在哪。
  **敏感值不进仓库，必须提供 `.env.example` 模板。**
- `C8 权限与安全边界`：谁能看/改什么，**服务端在哪一行代码上做裁定**，
  数据隔离范围（个人/部门/部门及下级/全域），越权时的行为。
  空权限范围必须返回空结果，而不是因过滤条件为 null 而放开全部数据。
- `C9 观测与日志契约`：打什么日志、什么级别、**哪些字段必须脱敏**（身份证/手机号/姓名/未成年人信息）。
- `C10 交互与呈现契约`：页面清单、设计 token（颜色/间距/字阶/圆角/阴影）、
  组件状态矩阵（`variant × size × state`，漏格=漏需求）、类型阶梯。实现只引用 token，**禁止裸 hex/px**。
  无 UI → `N/A（理由）`。
- `C11 AI/Prompt 契约`：prompt 模板、输入输出 schema、模型与参数、失败与降级路径、
  幻觉/越权的兜底。无 AI → `N/A（理由）`。

> **契约必须可执行**：每个契约条目要么是表格，要么是代码块/schema。
> 散文式的「接口应返回用户信息」不算契约，lint 会拦。

---

## 阶段 5 · 写 T 层（Tests · T1–T5）

- `T1 验收清单`：**每个 R-ID 必须有一行**——`R### | 验收方式 | 判定标准 | 检查者(人/脚本/测试)`。
  验收方式要具体到能执行：`单测 auth::test_dept_scope`、`e2e: 用部门账号登录后列表只含本部门`。
  **没有验收方式的需求 = 不算需求**，回 P5 补或删。
- `T2 指标与阈值`：性能、覆盖率、视觉 diff 阈值、错误率等，每项给数字。
- `T3 停工线`：**出现即停、不得继续施工**的信号。例如：
  权限过滤被绕过、真实个人信息进了日志或仓库、数据不可逆丢失、
  核心不变量被破坏、测试为了通过而被改宽。
- `T4 交付前置（DoD）`：交付前必须全绿的清单（构建通过、覆盖表全绿、
  lint 全绿、`.env.example` 齐备、文档与实现一致、根目录干净）。
- `T5 施工范围与里程碑`：`M0/M1/M2…`，每个里程碑给：包含的 R-ID、出口条件、明确不含什么。
  存量项目的 P0 修复排 M0。

---

## 阶段 6 · 完备性门（Completeness Gate · 硬闸门，不过不冻结）

三道，全过才准进施工。

**① 机检 lint**
```bash
bash <SKILL_DIR>/scripts/pact-lint.sh PACT.md --level=full
```
检查：四层锚点齐备且非空 · 无占位符（TBD/待定/XXX/`<填写>`）· 每个 R-ID 都被 T1 验收覆盖 ·
每个 D-ID 都有「理由」与「已否决」· C1/C4 含表格或代码块 · 头部有创建日期与四层映射表。
**FAIL 必须修到 PASS，不许豁免。**

**② 自查反扫**
拿全部输入物料（简报、既有 PRD/SDD、访谈记录、存量代码）**反向重扫**对照 PACT，
列出 未覆盖 / 部分覆盖 / 偏离 的条目。有则补。

**③ 零知识冷读门（Cold-Start Gate）—— PACT 的定义性检验**

这是把「只读这一份就能开工」从口号变成可验证的一步：

> **另起一个全新的 agent，只给它 `PACT.md`（不给对话历史、不给其他文档、不给代码库背景），
> 让它输出：(a) 它理解的实现计划；(b) 它必须追问才能动工的问题清单；(c) 它发现的矛盾与歧义。**

用 `Agent` 工具（`subagent_type: general-purpose`）跑，prompt 见
`templates/cold-read.md`。判定：

| 冷读结果 | 判定 |
|---|---|
| 追问清单为空，且实现计划与你的意图一致 | **PASS** → 可冻结 |
| 它提出的问题，答案其实**已在 PACT 里** | 不算漏洞，但说明**表述不可发现** → 调整结构/加索引后重跑 |
| 它必须追问背景、意图、或任何约束 | **FAIL** → 每个问题回填到对应锚点，重跑 |
| 它的实现计划**跑偏**（做了你没要的、漏了你要的） | **FAIL** → 说明 P4/P5/P6 表述有歧义，改写后重跑 |

报告与历次结论追加到 `.pact/cold-read.md`。**跑到 PASS 为止**，不许"差不多了"。

> 冷读门至少跑一次；`--level=full` 的新项目建议跑两个视角：
> 一个「实现者」视角（能不能开工），一个「反对者」视角（这规格哪里自相矛盾/不可实现）。

**冻结**：三道全过 → 在 PACT 头部标 `状态: 已冻结 · <日期>`，写入 `.pact/board.md`。
此后 PACT 的任何改动**必须**在 `.pact/changelog.md` 记一行（改了什么、为什么、影响哪些 R-ID），
并重跑受影响部分的 lint。

`--review` 模式：冻结前停下等用户确认。

---

## 阶段 7 · LOOP：按 R-ID 施工 + 自动验收（自驱迭代直到全绿）

工作单元 = **一个里程碑内的一簇 R-ID**（有 UI 时通常 = 一个页面 + 其 R-ID 簇；
存量项目 = 一个 P0/P1 优化项）。按 `T5` 的里程碑顺序取下一个未完成单元。

每轮：
1. **实现**：代码落到 `A2` 约定的位置；配置进 `C7` 的集中配置层，业务代码里零散落魔法值；
   每段标注覆盖的 `R-ID`。**禁止写没有 R-ID 的功能**——要加功能先回 PACT 加条目并记 changelog。
2. **验收**（读 PASS/FAIL，不靠肉眼）：
   - 跑 `T1` 为该簇 R-ID 指定的验收方式（单测/e2e/脚本断言）。
   - 有 UI 时追加三道（脚本已随本 skill 附带）：
     ```bash
     bash <SKILL_DIR>/scripts/token-lint.sh <组件目录>        # 裸 hex/px/rgb 必须为 0
     node <SKILL_DIR>/scripts/visual-diff.mjs <页面URL> <基准图> --threshold=0.02
     # + computed-style 断言，模板见 <SKILL_DIR>/scripts/computed-style.spec.ts
     ```
     `visual-diff.mjs` 需要 `playwright pixelmatch pngjs`（首次还需 `npx playwright install chromium`）；
     项目若已有自己的视觉回归方案，直接用你自己的，别为跑这个脚本引入新依赖。
   - 核对 `C3 不变量` 中与本单元相关的断言仍然成立。
3. **判定**：任一 FAIL → 同一单元继续修，**不前进**；全过 → `.pact/coverage.md` 标该簇 `已验证`。
4. **停工线检查**：对照 `T3`，命中任一条 → **立即停**，narration 写 `needs input:` 说明命中了哪条。
5. 每轮末更新 `.pact/board.md`（做了什么、下一个、阻塞）；PACT 有变动同步 `.pact/changelog.md`。

> 施工中冒出**新的关键歧义**且无法合理假设 → 回阶段 1 提问（`needs input:`），
> 别带着猜测硬写；能合理假设的标 `假设` 继续，不停。
> **节奏**：连续工作，不必睡眠。仅在等构建/dev-server 时用 `ScheduleWakeup`
> （短轮询 ≤270s；长等待 ≥1200s）并回传同样的 `/pact` 入参续跑。

---

## 阶段 8 · 收尾自检门（全绿才算完成）

1. `.pact/coverage.md` 无 `状态≠已验证` 的 R-ID；`T1` 每条验收都真跑过并贴结果。
2. 拿 PACT 原文**反向重扫**实现，列出 未覆盖/部分覆盖/偏离。
3. `T2` 各项指标达阈值（有 UI 则逐页贴 diff% vs 阈值）。
4. `T4` 交付前置清单逐条打勾。
5. **规格与实现一致性**：PACT 里的契约（表结构/接口/错误码/配置）与代码实测一致；
   不一致 → 要么改代码，要么改 PACT 并记 changelog，**不许放着**。
6. **骨架自检**：`PACT.md` + `CLAUDE.md` + `README.md` + `.pact/{interview,cold-read,board,coverage,changelog}.md`
   齐备且非空；`pact-lint.sh` 全绿。
7. **可接手性自检**：`A2` 与实际目录一致、配置集中且有 `.env.example`、
   新人照 README 能一把跑通、无孤儿/临时/死文件、无 TODO 占位、
   **根目录仅入口文件 + 业务目录**（散落文档/清单/图片已归位）。
8. **末次冷读**：对最终 PACT 再跑一次冷读门确认它仍描述的是现在的系统（防止规格与实现漂移）。
9. 全绿 → 输出 `result:` 一行总结。否则回阶段 7 继续 loop。

---

## 贯穿纪律

**规格纪律**
- PACT **自足**：不靠外链、不靠对话历史、不靠"你懂的"。引用外部文档时必须摘录关键结论进正文。
- **决策留痕**：每个非平凡取舍都有 D-ID，且写清**已否决方案与否决理由**。
- **可追溯双向**：R-ID ↔ 代码位置 ↔ 验收方式，三者互指。
- **冻结后走 changelog**：规格漂移是最贵的债，改动必须留痕。
- **文档必须带日期**：`创建日期` 必填，实质性修订加 `更新日期`。没有日期的规格无法判断时效。

**工程纪律**
- 遵循现有项目结构；空项目才提新约定。一文件一职责，按特性/领域分目录，
  不堆 `utils`/`helpers` 万能文件。
- 配置分层且各有单一来源；敏感值不进仓库；业务代码零硬编码魔法值。
- 权限过滤必须在服务端查询层；前端菜单/按钮隐藏**不是**安全边界。
- 涉及个人信息（身份证/手机号/姓名/未成年人/案件）一律脱敏，测试与文档里不得出现真实数据。

**输出纪律（这是后台 loop）**
- 每轮开头一句要做什么，结尾一句结果 + 下一步。
- 关键数字（覆盖率、lint 结果、各页 diff%）用自己的话复述，别只丢工具输出。
- 有未决**阻塞**时 `needs input:` 独占一行说明缺什么，并用 `AskUserQuestion` 给选项。
- 完成时 `result:` 独占一行、自包含。被用户打断则先复述其指令再行动。
- **默认不提交代码**：除非用户明确要求，改完停在未提交状态。
