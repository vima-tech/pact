# 存量代码八维评估（Brownfield Assessment）

> 创建日期: <YYYY-MM-DD>
>
> 触发：/pact-new 时项目已有实质代码。**只读、不改码。** 证据一律用 `file:line`。
> 严重度：**P0**=正确性/安全/约定冲突（必修） · **P1**=缺失需求/显著技术债 · **P2**=打磨。
> 结论要回流到 `PACT.md`：现状约束 → `P7`；结构 → `A2`；P0 → `T5` 的 M0；冲突处置 → `A5` 决策。

## 实现盘点

| 模块 | 语言/框架 | 规模 | 职责 | 与本次需求的关系 |
|---|---|---|---|---|
| `apps/xxx` | Vue 3 | ~8k 行 | 管理端 | 需接入新页面 |

## 八维评估

| 维度 | 评分 A–D | 现状 | 关键问题 | 证据 `file:line` | 最高严重度 |
|---|---|---|---|---|---|
| 1 结构与分层 | | | | | |
| 2 **核心不变量**（静默腐蚀点） | | | | | |
| 3 既有约定/裁定在代码里是否被遵守 | | | 冲突 = P0 | | |
| 4 数据正确性铁律（精度/单位/时区/校验/字符归一） | | | | | |
| 5 安全与权限（RBAC、隔离、脱敏、留痕） | | | | | |
| 6 可维护性与技术债（重复/死代码/配置散落/耦合） | | | | | |
| 7 性能与可扩展（分页/N+1/大数据路径/热点） | | | | | |
| 8 测试与文档（关键逻辑有无测试、能否接手） | | | | | |

## 关键发现（按严重度）

- **P0**：<问题 + 证据 `file:line` + 为何阻塞>
- **P1**：<>
- **P2**：<>

> **结构整理是 P1，不是 P2**：根目录散落文件、文档与实际不符，属「影响可接手性」，
> 必须在本轮处理，不得降级为可推迟的打磨项。

## 回流到 PACT

| 发现 | 回流到哪个锚点 | 已回流 |
|---|---|---|
| 既有 `xx` 表不可改 schema | `P7` 约束 + `C1` | ☐ |
| 权限过滤散落在各模块 | `A4` 原则 + `T5` M0 | ☐ |
| 与新需求冲突的存量设计 | `A5` 新增 D-ID | ☐ |

## 结论

> 现状判定；进入下一阶段的硬门槛；P0 清单是否已排入 `T5` 的 M0。
