# CHANGELOG

> 创建日期: 2026-07-26
>
> 升级：`npx skills update pact`（或重新 `npx skills add vima-tech/pact -g`）

## 2026-08-01 · 估算法补「多人并发」章节

原方法只覆盖单人/小团队，被问到「加人能不能压缩工期」时无从下手，
容易顺口答成「3 人快 3 倍」——那个数字通常错得离谱。

- `references/effort-estimation.md` 新增 **§五点五 多人并发**：
  · 收益衰减的两个结构性上限——**阻塞等待不随人数减少**（交付周期一半压不掉）、
    **关键路径地板**（沿 `P5` 依赖列做最长链分析，无论多少人都突破不了）
  · Amdahl 快查表：`开发跨度(n)=S+(D−S)/n`、`交付周期(n)=开发跨度+D×(B−1)`；
    典型形态下 2 人省 21%、**3 人省 28%（甜点）**、4 人 32%、6 人 36% —— **别指望线性**
  · **公共模块按传递扇入识别**，不靠感觉：从 `P5` 依赖列建反向图跑传递闭包，
    Top 3 的扇入常达全部需求的 40–60%
  · 六条防冲突策略：公共地基不并行 / 垂直切分禁水平切分 / 契约冻结先于并发 /
    `pact-trace.sh` 每日跑 / `C3` 不变量做成 CI 护栏 / 按依赖层排波次
- `S4 算阻塞缓冲` 加挂钩：考虑并发时在该步之后加做 §五点五，
  **别先承诺「加人快 N 倍」再回来算**。
- 反模式加两条：14 以为加人能线性压缩工期；15 并发前不冻结公共契约。
- `templates/estimate.md` 新增 §10 并发方案（天花板表/关键路径地板/公共模块清单/
  六策略落地/置信度声明）。

**置信度**：并发部分（Amdahl 串行比、沟通开销、集成返工率）**是推导值不是实测**，
文档三处都写了「先按 2 人跑完第一个里程碑回填实际并行效率，再决定加不加第 3 人」。

## 2026-08-01 · 内建 AI 辅助开发的工期估算门（S7）

PACT 此前能把「做什么、算完成」写死，却答不了「多久能做完、报多少钱」——
于是 `P7` 里出现了「工期约 6 个月，团队 5–8 人（假设，未经确认）」这种拍脑袋数字。
这类数字一旦进合同就是承诺，是规格里最贵的一处空白。

**做法：把估算门并进 `S7 写 T 层`，不新增工序号。**
理由是它的输入输出本来就在 T 层——可验收条目取 `P5`+`T1`、三条线写回 `T5`、停工线写回 `T3`。
硬塞一道 S7.5 只会打乱 `pact-status.sh` 的 S0–S11 序号校验，且名不正。

- 新增 `references/effort-estimation.md` —— 方法本体：
  **50% 法则**（AI 骨架恒定只覆盖约一半，且写更好的文档提不上去，边际收益极低）、
  降本推导 `剩余时间比 = w(1−r₁)+(1−w)(1−r₂)`（熟手 55–65% / 生手 ~30%）、
  分环节系数（自有二开 ~80%，**他人遗留改造可能负收益**）、
  按**模块当量**估算而非人日、阻塞缓冲 ×1.5–2（**取并行最大值不求和**）、
  三条线、按可追溯性排的停工线、13 条反模式、商业换算、校准回填。
- 新增 `templates/estimate.md` —— 固定输出格式，含校准回填表。
- `SKILL.md` 新增**协议七：估算门**，禁令加两条（禁止不走估算门给数字、禁止用骨架完成线对外承诺）。
- `pact-lint.sh` 新增**第 10 项 WARN**：`P7` 声称了工期/团队却没有 `.pact/estimate.md`。
  只 WARN 不 FAIL——纯内部项目可以不估算，但「有数字没依据」必须被点名。
- `pact-status.sh` 新增软检查：`S7` 已完成但缺 `.pact/estimate.md` 时提示。
- `references/agent-protocol.md` 的 S7 工序卡加估算门动作与两条新的偷懒模式。

**最反直觉、也最要防的一条**（已写进方法文档、协议七、工序卡三处）：
**PACT 写得好不是下调工期的理由。** 规格从 60 分到 90 分只把骨架覆盖率从 50% 提到约 55%。
PACT 消除的是方向性返工与验收扯皮，**不是编码量**——时间预算必须显性留给后 50%。

系数来源：vima tech 一线实测（HIS/政企方向，熟练使用 AI 的单人/小团队），
非本地实测部分已标注为先验；快查表标定到 12 个中等模块，超出须声明外推倍数。

## 2026-08-01 · 知识库改为自研单文件 HTML，去掉 mdbook 依赖

上一版用 mdbook 渲染，四个问题同时暴露：**样式受 Handlebars 模板与既有 DOM 约束**、
**布局固定（无当页目录、宽屏浪费、窄列 CJK 竖排）**、**交互只有全文搜索**、
**要装两个 Rust 二进制才能看**——而规格是要发给甲方与分包方的东西，对方不会为此装工具链。

**改法：渲染层自研，真源与 md 投影一个字不动。**

- 新增 `scripts/pact-book-html.mjs` —— 从既有数据模型直接渲染**单文件 HTML**：
  三栏布局、**字段感知搜索**（`ms:M3` / `src:SRC-B` / `类型:权限`）、★ 与里程碑过滤芯片、
  R-ID 悬停预览卡、当页目录联动、明暗主题、打印样式。
- **依赖图不再用 mermaid**（3MB 运行时）：图数据本来就在手上，直接出**可点击 SVG**，额外开销 0。
- 唯一 vendored 的是 `vendor/marked.min.js`（MIT，40KB）——章节页搬运 `PACT.md` 正文，
  作者可写任意 markdown，这块手搓解析器必踩坑；其余页面全部由数据模型生成。
- 产物：`.pact/pact-book/pact-book.html`（约 1.5MB，**零外部请求**，`file://` 双击即开）。
  `book.toml` / `SUMMARY.md` 仍生成，**mdbook 从必需降为可选**。
- `--check` 的比对范围加入 `pact-book.html`（它随 `PACT.md` 变，属投影）。

**分工没变、也不能变**：`src/**.md` 仍是 **AI 施工读的东西**，HTML 纯粹面向人。
换渲染器对 AI 侧零影响——这正是当初坚持「真源单文件 + 生成视图」换来的余地。

**过程中修掉的真缺陷**（都只有真在浏览器里跑才暴露）：
① marked v15 起不再自动给标题加 id，当页目录会整个空掉 → 自己补 id 并去重；
② 章节页 H1 与正文首个 `##` 重复出标题 → 生成时剥掉正文首行标题；
③ 窄列里 CJK 被逐字断行（「功能」竖成两行）→ 给短单元格精准打 `nowrap`，
   而不是用 `width:max-content`（那会让两列小表也被迫横向滚动）。

## 2026-08-01 · 交付物升级为 md 知识库 `.pact/pact-book/`

`PACT.md` 长到 2600 行 / 235KB 之后，两个真实痛点冒出来：
**AI 施工一条需求要吞整份 235KB**，**人想查「这条为什么这么定」要在长文里翻**。
但把真源拆成多文件会直接摧毁 PACT 的立身之本——「只读一份就能开工」的冷读门。

解法是**真源不动，加生成视图**：

- **新增 `scripts/pact-book.sh` + `pact-book.mjs`**（零依赖 node）——从 `PACT.md` 生成
  mdbook 兼容的 md 知识库，四类视图：
  | 视图 | 位置 | 给谁用 |
  |---|---|---|
  | 章节页 + `SUMMARY.md` 导航 | `src/{p,a,c,t}/` | 人：按四层读设计 |
  | **每条需求一页** | `src/r/R###.md` | **AI：施工只读这一页**——需求+验收+依赖+被依赖+相关决策+契约位置全聚合 |
  | 里程碑工作包 | `src/m/M#.md` | 领活的人或 agent：该阶段全部需求 + 出口条件 + 明确不含 |
  | 反查索引 | `src/idx/` | R-ID / D-ID（含**已否决**）/ 不变量 / 来源 / 依赖图谱 / 健康度 |
- **协议四由「三道机检」升为「四道机检」**，新增 `pact-book.sh --check` 查生成物漂移
  （手改生成物、改了真源忘重生成、残留孤儿文件，三种都抓）。
- **S8 退出条件加第 ④ 项**，`S11` 收尾门要求四道全绿；`board.md` 模板加对应闸门。
- **新增第 11 条禁令：禁止手改 `.pact/pact-book/`**。理由写在 SKILL.md 里——
  PACT 自己第 9 条禁令就是「规格漂移是最贵的债」，两份手写内容必然漂移。
- **顺带产出结构性体检**：`idx/健康度.md` 报 lint 查不出来的图连通性问题——
  无 `T1` 验收的需求、**未排入任何里程碑的需求**、指向不存在需求的依赖、缺「已否决」的决策。
  在真实项目（218 R-ID）上首跑即抓出 23 条需求漏排里程碑。

**边界（写死在文档里，防止后人误解）**：知识库是 `PACT.md` 的**投影**，不是真源；
**冷读门永远只读 `PACT.md`**。若某条信息只有在知识库里跳三页才拼得出来，
那是 `PACT.md` 写漏了，不是知识库的功劳。

## 2026-07-26 · 加 `--help`

面向人类用户的用法入口。此前只有 README（面向仓库访客）和 SKILL.md（面向 AI，
500+ 行执行细则），装完 skill 的人想快速搞懂怎么用没有合适的入口。

- **协议零：`--help` 短路** —— `$ARGUMENTS` 含 `--help` / `-h` / `帮助` / `怎么用` 时，
  只输出使用速览然后停止：不判模式、不建 `.pact/`、不碰 `PACT.md`、不进任何工序。
  放在执行协议最前面，先于其余一切判断（否则 AI 会把 `--help` 当简报去跑 S0）。
- `references/help.md` 人类向速览十节：怎么开口 / 五种模式 / 你要参与的三个时刻 /
  会生成什么 / 代码标注约定 / 三道机检 / 断点续跑 / 十二道工序一览 / 实用提醒 /
  **什么时候不该用 PACT**。
- `scripts/pact-help.sh` 终端里也能看（`--raw` 输出纯 markdown 便于管道），
  页脚附上本机实际安装路径与三道机检命令。

## 2026-07-26 · 落地侧补上机检（可追溯性）

此前生成侧有两个机检脚本（规格完备性 + 工序状态），**落地侧一个都没有**——
"代码是否真按 PACT 实现" 完全靠 AI 在 `coverage.md` 里自填，无任何交叉验证。
这让"严格按 PACT 执行落地"实际上是靠自觉的。本次补齐。

**新增**
- **R-ID 代码标注约定**：每段业务代码必须带 `// @pact R001`（多个写 `R001,R002`；
  Python/Shell 用 `#`，模板用 `<!-- -->`）。写进协议五禁令 4、S10 动作清单、
  项目 `CLAUDE.md` 模板。
- `scripts/pact-trace.sh` 落地机检（零依赖）：交叉比对 **规格 P5 ↔ 代码 @pact 标注 ↔
  coverage.md**，报四类问题——
  **虚报**（覆盖表说已验证但代码里没有，FAIL）·
  **野生功能**（代码标了规格里没有的 R-ID，FAIL）· 漏登记（WARN）· 未实现（INFO，
  `--require-complete` 下升为 FAIL）。并输出 R-ID → 代码位置反查表与实现进度。
- 协议四由「两道机检」升为「**三道机检**」，各自职责在 SKILL.md 列表说明。
- S10 每轮验收、S10/S11 退出条件、`coverage.md` 收尾自检均接入 trace。

## 2026-07-26 · 工序协议强化

在 v1 基础上把「描述性流程」固化为「不许跳步的强制工序协议」，并修掉首版的三个缺陷。

**新增**
- `SKILL.md` 前置「执行协议」六节，优先级高于其余描述：
  S0–S11 十二道工序总表（进入条件 / 必须产出 / 可机检的退出条件）· 每轮三件事 ·
  断点续跑规程 · 两道机检 · 十条禁令 · 何时该停下来问人
- `scripts/pact-status.sh` 工序状态机检（零依赖）：工序表完整性 · 状态词合法且跳过带理由 ·
  顺序合法 · 骨架按进度动态要求 · 冻结一致性 · 覆盖一致性；并输出下一道该做什么
- `references/agent-protocol.md` 十二张工序卡：动作清单 / 退出判定 / 该道最常见的偷懒模式
- **S10-CR 需求变更流程**：冻结后用户改需求必走的五步（回 P5 → 补 T1 → 改 C 层 →
  记 changelog → 判断是否重跑冷读门），堵住"绕过规格直接改代码"这个最常见的破防点
- `templates/board.md` 重写为工序状态表，作为断点续跑的第一真相源

**修复**
- `pact-status.sh` 冻结一致性在 `--build` 模式下误报 FAIL
  （S9 标「已跳过」是该模式的正常形态，不应双向强检）
- `pact-status.sh` 状态词判定过严：「已跳过 —— 理由」这类不带括号的写法被判非法，
  改为前缀匹配 + 理由可写在状态格或备注列
- `S8` 文档里把 lint 档位写死成 `--level=full`，feature 档项目照抄会用错档位
- `pact-status.sh` 头注释里的检查项顺序与实际输出顺序不一致
- 两个脚本 `-h` 的帮助文本打印范围溢出到代码行

## 2026-07-26 · 首个版本

- `PACT.md` 30 锚点单文件完备规格骨架（P1–P8 / A1–A6 / C1–C11 / T1–T5）
- `scripts/pact-lint.sh` 规格完备性机检九项：锚点齐备 · 无占位符 ·
  R-ID 被验收覆盖 · 决策含「已否决方案」· 契约可执行 · 文档带创建日期 等
- 零知识冷读门（`templates/cold-read.md`）：另起只读 PACT 的 agent，
  其追问清单即规格漏洞
- 五种模式 `--new / --feature / --merge / --build / --audit`，两档完备度 `full / feature`
- `references/authoring-guide.md` 逐节精度标准 + 反例；
  `references/example-PACT.md` 通过全部机检的完整范例
- UI 项目可选校验：`token-lint.sh` · `visual-diff.mjs` · `computed-style.spec.ts`
